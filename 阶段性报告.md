# 任务生成报告

当前阶段主要重写了任务生成的架构，以支持生成更复杂的具有文件、数据依赖关系的题目。在现在的测试阶段，我根据GDPVAL中的 “跨国音乐巡演利润汇总” 题目提炼和补充了一系列考点，进行题目生成的测试。题目成功生成，我用该题目分别对GPT-5-mini、Kimi-k2-thinking和Gemini-3-Pro进行了测试，结果如下：

| 模型 | 结果 |
| --- | --- |   
| GPT-5-mini | 19/27 分 |
| Kimi-k2-thinking | 13/27 分 |
| Gemini-3-Pro-Preview | 26/27 分 |

- GPT-5-mini：只懂一点会计业务
- Kimi-k2-thinking：完全不懂业务，只会机械地计算
- Gemini-3-Pro-Preview：学霸。但忘记在整个表格的最底部写一个“Grand Total（总计）”行，导致扣分。

## 全新的SkillNode

### SkillNode的属性

1. 基本属性   
- `node_type`: 节点的身份定位。分为 base（数据源头）、mutator（业务计算/转换）、trap（异常数据破坏）、global（全局规范，无数据依赖）。  
- `max_instances`: 该节点在一个题目中最多能出现多少次。基石节点通常是 1，而通用陷阱（如缺失值）可以设为 99。
- `requires`和`provides`: 端口。比如 requires = ["in_amount"]，它定义了“我需要一个金额才能工作”，但不关心这个金额在哪个表的哪一列。这实现了考点间的解耦。
- `possible_successors`: 该节点可能的后继节点列表。指明它可以向下游孵化哪些节点，以及如何将自己的 `provides` 映射到下游的 `requires` 上。

2. 语义属性
- `prompt_intents`: 定义了节点的语义意图，是留有占位符（如 {exchange_table}）的业务指令，用于生成给 Agent 看的题干。
- `deliverable_constraints`：强制性的交付物要求（如“必须包含总结”、“必须命名为 X”）。
- `hidden_rubrics`: 评分标准。
  
1. 运行时状态
- `node_id`: 运行时唯一标识符
- `input_bindings`:插槽绑定记录。记录自己的 `requires` 端口到底插在了上游哪个节点的哪个输出上。
- `rendered_intents/constraints/rubrics`: 将占位符替换成具体值后的文本。

### SkillNode的函数

1. **resolve_input_port(self, in_port_name, graph)**
当一个节点需要工作时，它不知道数据在哪里。它会调用这个函数，沿着 input_bindings 逆流而上，找到最终的数据源头。上游节点会返回的 (Table_Name, Column_Name) 元组，告诉它数据在哪里。

2. **on_join_graph(self, graph)**
这是节点被成功添加到图谱时瞬间触发的函数。
- 它首先调用 resolve_input_port 锁定上游传来的真实物理表名和列名。
- 它决定自己要创建什么新表、新列。
- 它把这些真实的物理坐标，全部填入 prompt_intents 和 hidden_rubrics 的占位符中，生成最终的文本。

3. **register_data_operations(self, vfs, graph)**
这是交给 VFS（虚拟文件系统）执行的函数。节点在这里对真实的底层数据进行操作。
- 对于 Base 或者 Mutator 节点，它会调用 `vfs.create_tabular_blueprint()` 注册新表，或者通过 `add_column` 追加新列，并配置对应的生成器（如正态分布、常数等等）。
- 对于 Trap 节点，它会调用 `target_bp.inject_trap()` 向特定的表和列压入破坏指令（如改成空值、加上负号），并偷偷把被破坏的行号记录在 VFS 的 ground_truth 里。

### 如何从Base节点开始构建整个任务图

当前采用“贪婪生长模式”，即从一个基石节点开始，把所有能加入的节点都加入进来，直到没有更多节点能加入为止。

1. 加入了一个节点。
2. 根据 `possible_successors` 列表，依次添加可能的后继节点到“候车室”，并把当前节点的特定输出与它们的对应输入绑定。
3. “候车室”里面如果有节点的输入端口已经全部被满足了，就把它加入到图里，并触发它的 `on_join_graph`，让它锁定输入数据的物理位置，生成自己的文本。否则，继续候车等待。
4. 循环，直到候车室里没有输入端口全被满足的节点了。

```
[ GlobalRequirementSkill: 全局规范 ] ──(手动空降, 独立节点, 控制排版与总结)

[ BasePnLSkill: 基石节点 (数据源头) ]
  │
  ├─( Expenses)─────>  [Trap: 双重负数陷阱_inst_1]
  ├─( Expenses)─────>  [Trap: 缺失值陷阱_inst_1]
  ├─( Gross_Revenue)─> [Trap: 缺失值陷阱_inst_2]
  │
  ├─(传出 Revenue + Country)
  │      ↓
  │   [ TaxMutatorSkill: 跨国扣税节点 ] 
  │      │
  │      ├─(算完传出 Net_Revenue)
  │      ↓
  ├─(传出 Country)
  │      ↓
  └── [ CurrencyExchangeSkill: 汇率换算节点 ]  <── (多父节点依赖)
```