# 工作汇报总结

## 考点提炼流水线

- 废除了僵化的 data_params (如数据具体是怎么生成的)，让大模型输出具有业务厚度的自然语言表结构与上下文。

- 废除提炼考点阶段的静态 Rubric 文本生成。将考点评测标准划分为 Fact(事实)、Reasoning(推理)、Robustness(鲁棒性) 三个维度，考点提炼时不需要产出rubric, 只需要给出这三个维度的评估标准应当怎么制定。

- 底层接口 (API) 防弹加固： 重写了 skill_api_tools，引入了容错加载机制和 kwargs 参数黑洞，完美解决了新老架构交替时的参数不匹配报错，实现了 19 个复杂节点的高质量极速入库与拓扑连线。

- 解决了新老架构交替时的参数不匹配报错。

## 考点组装流水线

### **阶段一：整合题目需求与内容**

**输入：** 一些考点  

**输出：** 
- 数据生成指令
- 无剧透的考题文本（用于最终的题目呈现）
- 有剧透的考题文本（用于内部参考）
- 所有考点的打分模板
- 题目基本信息（题目类型、概述、交付物等等）

### **阶段二：造题目数据**

**输入：** 数据生成指令 + 题目基本信息

**输出：** 充满业务厚度且带有陷阱的真实数据文件。

### **阶段三：做题**

**输入：** 无剧透的考题文本 + 有剧透的考题文本 + 数据文件

**输出：** 题目答案 + 过程记录（代码、工具调用记录等等）

### **阶段四：完善rubric**

**输入：** 题目答案 + 所有考点的打分模板 + 无剧透的考题文本

**输出：** 完善的rubric文本

### **阶段五：打包**

**输入：** 无剧透的考题文本 + 完善的rubric文本 + 数据文件

**输出：** 最终的题目

