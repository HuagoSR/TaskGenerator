# 考点提炼流水线

- 输入：一段自然语言描述的任务  
- 输出：新的考点

## Agent 0：陷阱注入

在原本干净的业务需求中，加入陷阱元素，提升评测任务的真实感与难度。

## Agent 1：脱水与切片

剥离多余的业务叙述，将复杂需求“脱水”拆解为纯粹的、原子化的数据操作步骤，形成一个个独立的考点切片。

## Agent 2：端口推演

分析原子步骤间的数据流依赖，推演出每个节点的输入 和输出 端口，构建出考点图谱的底层 DAG 拓扑骨架。

## Agent 3：数据抽象

负责将具体业务参数（如国家、税率）抽象为 JSON 模板参数 (data_params)。同时负责“脑补”并构建具有真实业务背景的数据库表结构，确保生成的测试数据不仅仅包含计算列，还具备丰富的业务维度。

## Agent 4：算子匹配

判断该考点是否需要物理造表或破坏数据，并为其分配底层 Python 算子（如 MultiTableBaseOperator 或 CellPerturbationOperator）。若系统缺失相应算子，还会自动触发 Human-in-the-Loop 机制生成开发工单。

## Agent 5：设计评分规则

基于图谱数据字典，自动撰写符合行业标准、参数化的英文评分标准，涵盖格式检查、数学准确性等等。

## Agent 6：考点查重与注册

负责图谱数据库的原子性操作。在物理写入 JSON 题库前执行严格的查重校验，避免相同业务逻辑的考点冗余。

## Agent 7：考点连线

暂时采用“相同的端口名直接相连”的简单策略，构建考点间的数据流连接。

# 遇到的问题

- 大模型幻觉引发的系统崩溃
  - 考点的JSON格式具有严格的格式要求，大模型在处理深层嵌套 JSON 时有时候会放飞自我，生成不符合规范的输出。
- 生产的题目往往过于简单
  - 由于最终的题目是把一堆考点的语义组合而成，即便最后经过LLM的语言润色，还是会让题目看起来有点像一个简单的步骤列表，缺乏真实题目的复杂性和迷惑性。    
- Rubric的设置存在问题。
  - 目前的rubric设置的过于笼统，导致评分时无法准确区分答案是否正确
  - 目前的rubric还不够符合标准的行业规范