# TaskGenerator 项目概要

> 状态：`active / authoritative`；更新：2026-09-15。
> 本文是项目目标、当前证据、限制与下一方向的唯一宏观状态源；文档入口见 [docs/README.md](docs/README.md)。

## 1. 目标与当前结论

项目目标是自动制造真实、有自然复杂度、可解且可公平评分的多文件职业任务，而不是建设模型排行榜。候选包保持 GDPval 形状：`dataset_row.json`、`reference_files/`、`deliverable_files/`，教师 rubric、计算依据和运行证据与候选输入隔离。

当前优先开发路径是 **S1**：一个强作者接收完整生成合同和按需 Professional Skill，可使用工具与 self-check，提交后接受共同的机械与专业准入。历史多阶段流程 **P** 保留为研究参照。现有对照只说明 S1 明显更简单、快速，未观察到 P 足以证明的额外收益；不证明 S1 普遍更优或达到 production level。

当前最重要的质量目标是形成可跨职业、跨世界重复使用的制造方法。强弱模型分差只是辅助行为证据，不能替代真实性、可解性、rubric 公平性、失败率和生产成本。

## 2. 当前方法

1. 从公开职业触发与规则、已有 Professional Skill 出发，先建立自然业务世界和候选可见记录。
2. 作者生成任务题干、DeliverableContract、候选材料、教师监督、正式原子 rubric 和可重放计算依据。
3. 离线准入检查身份、路径、全部文件格式、日期/实体/金额、记录来源因果、计算、义务依据、合理替代表达和答案泄漏。
4. 冻结后才进行隔离 Solver 行为核对；关键职业结果是教师侧 rubric 诊断映射，不是第二套分数或隐含义务。
5. rw-task 只作为末端交付与模型差异诊断。legacy 整数部分分和高分复审不等于正式原子 rubric 评分。

不建设新的通用业务本体、evidence-graph 运行时、额外角色链或自动“分数→生成器”反馈系统；现阶段不做 RL、训练或公开题库发布。

## 3. 已获得的生产与使用证据

### 3.1 S1 可用性

- S1 已在审计、采购价格合理性和采购交付验收等多个新世界生成通过共同有限准入的任务包；这支持“方法可运行”，不是稳定成功率或专家质量证明。
- G1 提供一个正确隔离 blind-use 案例：Solver 只看到题干、8 份候选材料和提交说明，完成核心价格计算、跨来源比较及有边界结论。作者与 Solver 同源、仅单案例、无正式 Grader。
- 两次错误 G1 启动永久保留：`wrong_input_dataset` 跑了旧 rep2 输入；`wrong_input_prompt` 把作者提示发给 Solver。两次均计入真实消耗，不构成 blind-use 证据。

### 3.2 2026-09-14 两个四题小批

| 批次 | 首次位置 | 准入 | 结构失败 | 行为证据 |
|---|---:|---:|---:|---|
| T1–T4 难度小批 | 4 | 3 | T3 CSV 行缺字段 | 3 道准入题上，Sol 的三个关键结果均为 3/3；mini 为 0/3、1/3、1/3，属于案例级初步分差 |
| N1–N4 通用性小批 | 4 | 3 | N2 CSV 两行多尾随字段 | N1/N3/N4 完成 9 个隔离 Solver 会话及 legacy 诊断 |

两批均保留失败位置，不修题补位。准入率 `3/4` 只描述本次首次位置，不能外推为稳定生产率。三个关键结果的分母 `3` 是预先冻结的高价值职业观察点，不是完整 rubric 条数、百分比或正式得分。

### 3.3 N1/N3/N4 legacy rw-task 诊断

Solver 为 Tuzi/Stirrup 的 `gpt-5.6-terra`、`gemini-3.5-flash-lite`、`gpt-5.4-mini`；Judge 为 `gemini-3-pro-preview`。9/9 交付结构有效，0 Solver 会话重跑、0 人工介入。

| 任务 | Terra | Flash-lite | Mini | 主要解释 |
|---|---:|---:|---:|---|
| N1 审计资料可靠性 | 88 | 58 | 39 | 分数分层，但三模型均未完成冻结的总体/调整桥 |
| N3 采购价格合理性 | 100 | 85 | 71 | 三者均有实质缺陷；Terra 工作簿有 3 个公式错误却获 100 |
| N4 采购交付验收 | 100 | 96 | 50 | 与职业观察一致：Terra/Flash-lite 3/3，mini 0/3，是本批最清楚的案例级差异 |

9 次初评中 4 次触发条件复审，共 13 个完整 Judge 响应、294,316 reported tokens。另有 1 次 `503` 和 2 次 `502` 无有效响应；含失败共 16 次评分 HTTP 尝试。原 import-path 错误、post-response `criterion_id` 校验错误及所有恢复 scope 均保留。

这些分数是 **legacy rw-task diagnostic**：旧 grader 允许部分整数分，并会漏掉原生工作簿公式错误。它们不能覆盖文件 QA、不能替代正式原子 rubric，也不能从每格一次作答推出稳定模型排名或区分率。

## 4. 关于“任务为什么难”的当前证据

### 有直接案例证据支持

- **Evidence-conditioned judgment**：G1/W3 中，模型会随候选可见证据充分程度改变 freight scaling 的 epistemic status；A 保留 sensitivity assumption，B 在承运商正式事实出现后改为有事实依据的处理。
- **关系与局部状态更新值得研究**：G2 中模型能注意正式 correction 并保持无关限制，但个别链条出现 relation uptake/state rebinding 与下游 requirement 更新不完整。
- **行为测量必须落到职业产物**：是否维持 unresolved matter、是否提出匹配 follow-up、是否改变局部处置，比要求模型复述完整 tuple 或内部推理链更可识别。

### 尚未得到证明

- W2 的跨文件发现负担不是 `gpt-5.4-mini` 的明显行为瓶颈；不要继续靠换弱模型寻找失败。
- 目前没有证据证明“依赖图越深越难”。图上更深的事实可能在材料中直接陈述，graph depth 不等于 inference depth。
- G2 的部分结果存在 observability 或 negative-control ambiguity，不能据未外化中间关系直接判定内部 rebinding 失败。
- 单次 legacy 分差不能证明任务稳定有区分度；两模型共同失败也不能自动证明题目高质量或困难。

## 5. 可复用的制造与测量规则

- 先区分 **evidence-state uptake** 与 **disposition expression**。必需证据的存在/缺失、unresolved 状态和匹配 follow-up 必须可观察；除非合同明确规定，不把 `hold` 等标签当唯一答案。
- Rubric 每项对应一个候选可见、独立可观察的成果，允许等价表达和有依据的条件性结论；避免一个上游错误在多个下游条目重复扣分。
- 全格式检查覆盖 PDF、CSV、XLSX、DOCX 和 Markdown。不能只检查易提取文本；表格行宽、公式错误、缓存与视觉可读性均可能改变可解性。
- 记录必须有可信的 producer、record time、query scope、版本和 custody 因果。实验对称性不能凌驾于业务真实性；不要用空文件、占位值或不自然 joined export 强造 A/B。
- 普通任务允许自然信息冗余；只有受控实验才要求条件差异隔离。受控实验必须先审计所有 candidate-visible 格式中的目标事实。
- 输入身份必须在调用前从真实 `dataset_row.prompt`、task ID、输入树和模型配置核验。模型目录可见或历史成功不等于当前路由可用。

## 6. 执行与修复经验

- `wrong_input_dataset` 与 `wrong_input_prompt` 促成了启动前 identity gate；旧错误记录不得撤回或覆盖。
- Stirrup 的 1,000 字符 logger 截断是展示层行为；只有代码路径或实际命令证明 ToolMessage 被截断，才能判模型上下文异常。
- 本地修复必须先用真实失败产物回归：评分包装器先后暴露 `rw-task` import path 和 rubric 字段名假设问题；后续校验用真实初评/复审结构验证后再运行。
- Provider 的 `502/503` 计入尝试，即使无 usage；预检只证明当时能力可用，不证明整个评测窗口稳定。
- 技术恢复使用新的、输入/模型/环境/预算绑定 scope；保留父 scope、首次失败、响应和 receipt，不改写历史为成功。

## 7. 当前未决问题与下一方向

1. 如何让 S1 在不同职业持续生成自然、可解、rubric 公平的任务，同时降低 CSV/XLSX 等结构失败率。
2. 哪些职业负担能稳定地产生可观察行为差异：证据状态维护、关系更新、requirement-to-action，还是特定计算/总体重建。
3. 如何让正式原子 rubric 与原生文件 QA 共同工作，避免 legacy 文本 grader 的高分掩盖公式或交付缺陷。

下一阶段优先继续小批、跨职业首次生产：统一简短 S1 合同，按需使用现有 Skill，保留每个首次失败，先做离线准入，再用有限 Solver 行为证据筛选。不要围绕单题反复修补，也不要为了区分度牺牲职业真实性或测量公平性。

## 8. 文档与证据入口

- 当前架构：[system_architecture.md](docs/architecture/system_architecture.md)
- 接口索引：[global_interface_contracts.md](docs/architecture/global_interface_contracts.md)
- 生产/证据语义：[production_mvp_definition.md](docs/architecture/production_mvp_definition.md)
- 下一方向：[pipeline_reconstruction_optimization_plan.md](docs/architecture/pipeline_reconstruction_optimization_plan.md)
- 执行边界：[pipeline_reconstruction_runbook.md](docs/operations/pipeline_reconstruction_runbook.md)
- 行为测量：[behavioral_measurement_spec_v1.md](docs/research/behavioral_measurement_spec_v1.md)
- 历史研究与错误经验摘要：[r10_research_and_execution_lessons_20260914.md](docs/archive/r10_research_and_execution_lessons_20260914.md)

原始模型输出、任务包、日志和 receipts 位于 ignored `artifacts/`，不进入公共仓库。瘦身前活跃文档的精确正文保留于 Git 提交 `66ce931`。
