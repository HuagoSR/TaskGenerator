# TaskGenerator 项目概要

> 状态：active / authoritative；更新：2026-09-03。
> 唯一宏观状态源；接口见 contracts，执行边界见 runbook。

## 目标与方法

项目核心是自动制造真实、可解、可核验的职业任务，而不是建设独立排行榜。方法为：公开 Work Seed / Rules → 专业 Skill → 工作世界与候选材料 → Task Mining → Teacher Truth / Rubric → 静态与行为验收。

GDPval 仅作 eval_calibration_only：可以借鉴抽象方法，不能把其具体题目、rubric、Gold 或文件输入生成链。专业有效性为 provisional / LLM-proxy。

## 已建立的能力

- R9 完成过十题生产、三模型交付和双评委比较，证明基础设施可运行；R7 完全新生产因 provider 不稳定冻结于 5/10，不能拼接或恢复为评测。
- R10 建立公开种子、规则、Agent-native Skill、Scenario-First 和 World-First 编译，形成候选材料、交付合同与监督。
- R10.8 修正过采购验收的 teacher anchor：3/40=7.5%，超过 5%；随后仍出现评分饱和或 Judge 歧义，以 evaluator_revision_required 结束。
- R10.9 四个普通/强化 matched 任务完成静态、truth audit、三档校准与 8/8 Solver 交付，但正式 paired review 均有 Judge 歧义，尚不能判断自然难度改良效果。
- R10.10 V2.0 开发集稳定，但一次性留出验证未全部通过。公开 GDPval 三模型完成 36 份交付；原 60 次评分队列已停止，不恢复。

## 当前结果：精简评分已完成

独立三题开发集评分为 preliminary_scoring_complete：DeepSeek 主评九份，Terra 抽查三份，共 12/12 完成、13 次实际尝试。逐项给分后程序求和，没有额外整体偏好票；抽查不混入模型均分。

三题主评均分为 Sol 0.949942、Flash 0.918948、Pro 0.896056，只能作为探索性描述，不构成可靠能力排名或 evaluator_validated。

实际文件检查发现：

- 审计：主评以正确的期末余额支持“各月均正确”，遗漏了较早月份的余额/调节错误。核验范围必须与 rubric 量词一致。
- 合规：本地题干限定部分问题需要法规引用并明确一项例外，rubric 却增加额外引用及例外要求。两位 Judge 同意也不代表标准公平。
- 采购：对隐含的推广范围、结论位置和等价表达有不同理解，不能把这些都归因于模型能力。

原 rubric、题目、答案、评分和留出结果保持冻结。完整诊断与运行记录只保存在 ignored artifacts。

## 当前工作：把经验回流到造题

两题 rubric 生成验证已实现，正在进行测试，尚未外部执行：

1. 为编译器增加独立 TaskSpecificRubricV2，少量专业判断可以对应多个可核验评分项。
2. 每项先追溯工作要求，再定义满分、部分分、适用例外、等价表达和核验范围，不机械拆成固定比例。
3. 仅用 R10.9 审计 baseline、采购 baseline 两道开发题生成新 rubric；原题干、candidate、Teacher Truth、矩阵与旧 rubric 不变。
4. Terra medium 每题一次生成，官方 DeepSeek V4 Pro max 每题一次独立审查；正常四次 session，全批最多两次格式/传输补跑。
5. 产出两份中文检查材料后停止。不运行 Solver，不新增评分，不扩大题量。

发现监督或题干问题时只报告，不通过 rubric 掩盖。两题通过也仅说明编译方法初步可用，不意味着专业有效性获证。

## 环境与停止边界

沿用 huago-cone current milestone-r9-huago-cone-a2d8a5e35099，previous milestone-r9-huago-cone-746ff9b55f1b。本轮不构建、激活或修补环境；无漂移不重复公开探针。发布磁盘门槛仍为 95 GiB。

不改历史 Registry、Skill、任务及 Solver 交付，不触碰 Test/v2_outputs，不训练、不公开题库、不切换默认模型。用户本轮授权覆盖新的两题生成/审查 campaign，仍保存独立 scope/receipt。

文档入口见 [docs/README.md](docs/README.md)。
