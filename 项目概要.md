# TaskGenerator 项目概要

> 文档状态：`active / authoritative`
> 更新日期：2026-07-18
> 职责：唯一项目级状态源，管理项目目标、宏观路线、阶段结论和当前优先级。

## 一、项目目标

TaskGenerator 的核心目标是构建一个可批量运行、可治理、可验证的真实世界任务生产系统，为强化学习和其他训练流程持续提供高质量训练数据。

系统需要能够从真实材料出发，自动完成语义技能提炼、工作流条件化任务组装、参考文件生成、教师解答、训练标注、rubric、质量验收、rw-task 导出和评测反馈闭环。

项目的主要成果应理解为：

1. 自动任务生产系统；
2. 能够提升模型能力的训练数据系统；
3. 可复用的真实世界任务 benchmark 生成能力，但这只是附带能力，不是当前主要价值主张。

GDPVal 的角色是外部测试集和校准依据，而不是训练任务来源。最终需要通过训练实验回答“自动生成的数据能否提升模型在 GDPVal 等外部任务上的能力”。

## 二、系统边界

```text
E:\THU\2026Spring\SRT\
├── TaskGenerator\    当前核心仓库和任务生产系统
├── rw-task\          真实执行与评分框架
├── GDPVal\           外部校准与最终评测材料
├── papers\           论文与背景材料
└── 项目概要.md       兼容入口，只指向本文件
```

关键边界：

- `GDPVal` 仅用于 `eval_calibration_only`，不得流入训练任务生成。
- 真实评测复用 `rw-task` 的 package、runner 和固定 grader 链路。
- `.env`、API key 和 bearer token 只能通过本地文件或进程环境使用，不能进入 prompt、报告、日志、示例或提交。
- 外部模型调用授权按具体实验生效，不构成永久授权。
- 原始任务包、provider 输出、评分日志和大规模运行结果属于 artifacts，不是项目文档。

## 三、当前宏观架构

```text
Source / Skill / Resource Substrate
        ↓
Workflow / Motif / Task-graph Planning
        ↓
Task Package Generation
        ↓
Validity / Evaluation / Feedback / Promotion
```

### 1. Source / Skill / Resource Substrate

Pipeline A 从真实材料中提炼可复用语义资产，包括 source、semantic skill、typed resource、trace edge、motif hint 和 workflow context。该层以 LLM 语义抽象为主，但持久化和晋升必须显式、可审查。

### 2. Workflow / Motif / Task-graph Planning

规划层根据领域、workflow archetype、motif、graph role、资源兼容关系和采样约束，形成可执行任务子图。它负责避免退化为孤立技能拼接或单任务 prompt 调参。

### 3. Task Package Generation

Pipeline B 将任务子图转化为 blueprint、reference files、candidate prompt、GoldenRun、TrainingAnnotation、rubric、package 和 rw-task export。LLM 适合情境构造和教师候选，确定性代码负责验证、溯源、打包与门禁。

### 4. Validity / Evaluation / Feedback / Promotion

该层负责 verifier、quality gate、real-worldness、difficulty、model separation、production QA、release、promotion 和 rollback。静态检查只负责结构过滤，真实模型区分证据必须来自受治理的执行评测。

当前技术结构的稳定说明见 `docs/architecture/system_architecture.md`。

## 四、阶段台账

| 阶段 | 状态 | 核心结论 |
| --- | --- | --- |
| Phase 0–10 | 完成 | 建立全局接口、verifier、promotion、dashboard、model separation profile 和 eval orchestration 骨架。 |
| Phase 11 | 完成 | 首次形成可重复的 `candidate_ready` 生产路径，3/3 candidate-ready 且 verifier pass。 |
| Phase 12 | 完成 | 扩展到 5-case、10-case hardening；7/7 negative controls 被捕获；真实双模型 mini-campaign 完成。 |
| Phase 13 | 完成 | finance/audit governed production MVP 收口；最终 8/8 production candidate，strict reviewed release ready。 |
| Phase 14 | 完成 | GDPVal calibration 与 LLM shadow 证据闭环完成；GoodTaskProfiler 保持 observational。 |
| Phase 15A/15B | 完成 | 当前 reform 为 `hold_for_redesign`，不得推广默认链路。 |
| Phase 16 | 完成 | 四案例真实评测完成，最终为 `redesign_again`；Contract V2 不推广。 |

阶段计划、handoff 和报告统一保存在 `docs/archive/`。历史材料不再承担当前状态管理职责。

## 五、Phase 16 最终证据

执行范围：

```text
4 evidence_to_deliverable cases
3 arms
2 evaluated models
fixed grader = gpt-5.4-pro
completed records = 24 / 24
```

结果：

| arm | mean strong delta | mean weak delta | mean gap delta | positive gap cases |
| --- | ---: | ---: | ---: | ---: |
| `contract_v2_only` | `-0.0114` | `-0.0024` | `-0.0090` | `1 / 4` |
| `contract_v2_plus_productive_complexity` | `+0.0466` | `+0.0641` | `-0.0175` | `2 / 4` |

最终决策：

```text
phase16_status = complete
phase16_decision = redesign_again
promotion_decision = do_not_promote_default_chain
default_generator_change_allowed = false
four_case_clean_eval_proven = true
```

这说明 Contract V2 已缓解结构对齐问题，但没有形成稳定的四案例模型区分度。Phase 16 到此关闭，不继续局部调优 `evidence_to_deliverable`。

权威历史结论位于 `docs/archive/phases/phase16/PHASE_16_COMPLETION_2026-07-10.md`。

## 六、当前能力与瓶颈

### 流水线重构阶段性总结（2026-07-18）

| 方面 | 当前结论 |
| --- | --- |
| 重构实现 | R0–R5 的离线核心与 R7 report-only 核心已经实现；统一交付合同、LLM proposal-first 设计、V2 semantic artifact、行为失败归因、Validity/Utility 分轴和 matched comparison 治理均已进入代码与回归测试。 |
| 工程证据 | replacement-slice 后完整本地与固定 Linux/amd64 断网容器通过 288/288 tests；这证明合同回归与容器可复现，不证明模型能力或任务真实性。 |
| Provider 证据 | v23–v29 已在 policy、cross-check 和 fan-in briefs 上验证 semantic proposal、无损 normalization、feedback-conditioned repair、V2 materialization、内容与实际渲染审计；外呼只使用 `gpt-5.6-sol`，没有使用 `claude-sonnet-4-6`。 |
| 当前 campaign | V31 四个 matched briefs 已完成：8/8 provider assignments materialized；10 calls、148,709 tokens、1410.874 秒、20 USD，2 feedback retries、0 unconditioned retry。provider screening=`proceed_to_behavioral_evaluation`，12/12 route-blind staging pass，campaign=`evaluation_ready`。 |
| 尚未证明 | solver panel 仍阻断；没有盲包业务执行、repeated grader、独立专业复核、完整 12-task confirmation 或训练收益证据。V3 strong 结果也不能证明模型能力不足，因为它没有形成任何 tool call。 |
| 阶段判断 | V3 为 fail/pass/indeterminate：weak 实际调用工具但交付纯文本伪 XLSX，medium 正常完成，strong 的 provider transport/usage 正常却连续产生空 assistant response、0 tool calls，属于模型—网关—agent 协议待判定故障。未授权 replacement request `8b6c6aad...e65ab` 冻结为 superseded，不得创建 receipt。下一步并行推进一次限额式兼容性闭环和约 6-task 真实性金样本，而不是继续扩充治理层或直接换 strong 模型。 |

replacement-slice 实现快照的本地/固定 Linux/amd64 基线为 `287/287`；实现快照指纹 `e4c208f5...cb865`，parity report SHA `ec159667...64aa5`，容器断网、只读根、无 provider credentials 且 cleanup 成功。由于治理文档本身参与源码指纹，候选绑定与 exact request 必须使用文档同步后的 fresh parity，不得把该实现快照直接当成外呼授权；它也不授权候选模型。

当前已经具备：

- Pipeline A 的来源收集、技能抽取、review、registry 和 graph diagnostics；
- Pipeline B 从子图采样到任务包、验证、export、production QA 和 release 的模块化链路；
- Manifest V2、scratch-first、checksum、resume/rerun/status 和 external-effects ledger 支撑的本地统一编排；
- 固定 Docker、服务器候选激活、离线等价、恢复和 bounded public smoke；
- finance/audit 与 warehouse_inventory 两个受限领域切片；
- 多阶段静态门禁、真实 rw-task 执行、结果导入、失败保留和 promotion 判断；
- 重构 R0–R6 的离线治理核心：tracked 回归基线、统一 `DeliverableContract`、proposal-first design frontend、hybrid materializer、solver preflight、`BehavioralExecutionReport`、Validity/Utility 分轴、七维 rubric 和三路线 matched-comparison scaffold。

当前主要瓶颈不再是“能否生成任务”，而是：

1. `DeliverableContract` 与 delivery inspection 已通过本地/容器合同复验，但 agent adapter 尚不能可靠区分模型空响应、gateway tool-call 映射失败与真实能力不足；strong V3 未进入 sandbox，不能进入模型排名；
2. proposal-first 与 V2 hybrid materializer 已在 V31 获得四个 brief 的完整 fresh provider 观测；LLM-led 与 skill-guided 均为 4/4 materialized，且所有 materialized workbooks 的内容、显示、provenance 与 export identity 审计通过。该结果证明 provider/materialization 接口达到评估入口，不证明任务真实可解或任一路线更优；
3. productive-complexity、skill ablation 和七维 rubric 已有可审计合同，但当前证据仍是 structural proxy，不能替代独立 solver、repeated grader 与专业复核；
4. R6 的 8/8 provider assignments 与 12/12 route-blind staging 已完成；V31 仅完成公开工具预检且 panel 为 blocked，尚无盲包 solver、grader 或独立专业复核观察，因此不能比较路线优越性或形成训练准入结论；
5. 训练价值、广泛跨领域泛化和稳定人类专家证据均未建立，因此必须把 Validity、Utility 和最终训练价值分层证明。

修订后的执行顺序是：先用一次离线适配改造和最多一次 1–2 call 公共探针关闭 agent 协议问题；同时不等待完整 panel，先构建约 6 题真实性金样本并引入独立专业抽样；只有兼容性与真实性双门均通过，才恢复 12-task matched screening。R0–R5 接口默认冻结，新模块必须替代旧步骤或消除人工操作，测试数量不再作为主要进度指标。

## 七、A–F 路线

| 路线 | 状态 | 交付目标 |
| --- | --- | --- |
| A. Phase 16 证据收口 | `completed` | 四案例真实评测、最终 promotion 判断和阶段关闭。 |
| B. 文档和项目状态治理 | `completed` | 唯一概要、活跃文档收敛、历史归档和自动治理检查。 |
| C. 本地端到端自动化 | `completed` | scratch-first、可恢复、可重复的 source→skill→task→QA→eval preparation 统一入口。 |
| D. 服务器复现 | `completed` | 固定 Docker、候选激活、离线等价、恢复、batch 和 bounded public DeepSeek smoke 均通过。证据见 `docs/archive/phases/milestone_d/MILESTONE_D_COMPLETION_2026-07-10.md`。 |
| E. 第二领域垂直切片 | `completed` | warehouse inventory 离线对照、真实公开来源 DeepSeek extraction、污染门禁和服务器复现均通过。证据见 `docs/archive/phases/milestone_e/MILESTONE_E_COMPLETION_2026-07-10.md`。 |
| F. 训练数据准备度 | `pipeline_reconstruction_active / behavioral_executor_validated_evaluator_active` | 任务设计、Reality gate 和本地 Codex 行为执行主线已经闭合：最新 matched cohort 为 12/12 有效交付，两路线各 6/6。当前不再重构 solver，而是压缩和校准 grader、扩大代表性生产样本、收敛生成路线，再决定训练集准入；不进入 RL、不推广 E2D 或切换 production release。 |

旧 `finance_f4_from_scratch_validation` blocked campaign 保留为回归证据；F4.2 使用全新 source、registry 和任务结果重新验证。新 campaign `finance_f4_from_scratch_validation_02` 只收集一次四类公开资料，scratch registry 从零得到24项，随后按 `fan-in → cross-check → policy → E2D` 两轮交错逐题运行。8题最终均通过，canonical registry SHA 前后不变，Tuzi backup key 共20次调用、冻结价约¥0.783399。candidate release `f4-2-from-scratch-753a70c` 未激活，服务器 `current` 仍为 `milestone-f-data-production-6304e84`。

### 里程碑 C 最终结果

里程碑 C 已把既有 `v3_end_to_end_pipeline` 收敛为统一入口，并形成 Manifest V2、scratch registry、阶段 checksum、原子写入、resume/rerun/status 和 external-effects ledger。

| 验收运行 | candidates | accepted | sample-ready | candidate-ready | verifier/export | eval prepared/executed |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| offline smoke A | 4 | 4 | 3 | 2/2 | 2/2 | 1/0 |
| offline smoke B | 4 | 4 | 3 | 2/2 | 2/2 | 1/0 |
| public DeepSeek smoke | 5 | 3 | 3 | 2/2 | 2/2 | 1/0 |

两个离线 run 的 normalized content fingerprint 均为 `722497164acf1138bbfccd4975e9a0ed75ddf7adbc463738b3c90b9ab6881173`。真实 LLM smoke 只上传 tracked public synthetic package 进行技能抽取，没有 web collection、私有包上传或外部评测。

### 里程碑 D 最终结果

里程碑 D 已激活固定 Linux/amd64 release `milestone-d-094c6cb`。本地两次 offline smoke 与候选服务器 offline smoke 均精确复现 `4 / 4 / 3 / 2`、verifier/export `2 / 2`、eval `1 / 0` 和 normalized content fingerprint `722497164acf1138bbfccd4975e9a0ed75ddf7adbc463738b3c90b9ab6881173`。服务器 batch、真实中断恢复和 canonical registry 不变证据继续有效。

bounded public DeepSeek campaign 固定为 4 candidates、6000 completion tokens。首次运行由新诊断明确记录为 `finish_reason=length`；唯一一次同配置重试以 `finish_reason=stop` 完整返回，并得到 `4 candidates / 4 accepted / 3 sample-ready / 2 candidate-ready / 2 verifier-export / 1 prepared / 0 executed eval`。没有使用 mock fallback、JSON 修补、web collection 或外部 eval。

### 里程碑 E 最终结果

第二领域已固定为 `warehouse_inventory`，生成来源只使用 O*NET 与 FEMA 的公开材料摘录/结构化转述。离线 scratch-first run 得到 `4 candidates / 4 accepted / 4 sample-ready / 3 candidate-ready / 3 verifier-export / 0 QA blocked / 1 prepared / 0 executed eval`；同 commit 的 finance control 得到 `4 / 4 / 3 / 3 / 3 / 0 / 1 / 0`。canonical registry 前后 hash 不变，GDPVal 隔离污染台账为 `pass`，Milestone C fingerprint 仍精确复现 `722497164acf1138bbfccd4975e9a0ed75ddf7adbc463738b3c90b9ab6881173`。

真实公开来源 DeepSeek run `milestone_e_warehouse_llm_01` 首次即成功，无需重试：`4 candidates / 4 accepted / 3 sample-ready / 3 candidate-ready / 3 verifier-export / 0 QA blocked / 1 prepared / 0 executed eval`。Provider 为 `deepseek-v4-flash`，`finish_reason=stop`、`likely_truncated=false`，真实 LLM artifacts 的 GDPVal 污染台账仍为 `pass`。

Docker release `milestone-e-ed0568e` 已完成本地和服务器离线复现并激活为 `current`；public smoke 与 warehouse smoke 的 fingerprint 分别稳定为 `722497...1173` 和 `f4091e...bab3`，旧 `milestone-d-094c6cb` 保留为 `previous`。该结果只支持单个第二领域垂直切片通过，不构成广泛跨领域泛化或训练价值证明。

Milestone F 的早期财务审计数据生产形成了 60/60 candidate-ready 任务；其中 30 题的四模型差异评测也已完成。120/120 solver 进程均正常返回，无重试、无 provider 故障、无 OOM；`gpt-4o-mini` 有 4 次 `non_delivery`，因此有效交付物和主评为 116/120，复评为 63 份。四模型有效输出平均分依次为 `0.202 / 0.400 / 0.438 / 0.459`，弱模型与较强模型的整体差异清晰，且 26 个完整 panel 中无 rank inversion。但标签仅有 `1 informative`，并有 10/30 题 `grader_unstable`，因此该证据只支持“任务具有一定模型区分度”，不证明训练价值或 benchmark 权威性。评测已停止，不继续剩余 30 题；详细证据见 `docs/archive/phases/milestone_f/MILESTONE_F_MODEL_DIFFERENCE_EVAL.md`。

随后对固定抽取的 8 题完成 candidate-blind 人工式诊断。三类任务的主要金额、匹配和异常关系大多可以独立重算，候选 XLSX 也均可打开和阅读；但 `8/8` 任务都需要补齐候选可见合同：银行对账缺少账户余额，三单匹配缺少 `clear/hold/investigate` 的处置规则，费用题缺少执行“每人 100”阈值所需的就餐人数。`8/8` rubric 均无独立 fact-check 条目，直接交付要求仅占 `9/37`，并混入大量跨领域通用技能；这会系统性压低分数并放大 grader 差异。#13、#18、#35 有明确 grader 问题，gpt-4o-mini 另暴露未交付、0 字节文件和错误文件格式等真实 solver 能力失败。当前状态为 `human_review_completed_revision_required`：下一步先修任务合同与 rubric，并仅用 3–5 题小 cohort 验证修订效果，不进入 RL、不继续剩余 30 题。详细诊断见 `docs/archive/phases/milestone_f/FINANCE_30_TASK_HUMAN_REVIEW.md`。

Milestone F2 已在统一 DAG 中加入 `semantic_validation` 并完成真实校准。DeepSeek 对原始 8 题实现 `8/8` blocking recall，并发现全部三个已知问题家族；但严格 reason-code recall 为 `84.2%`，Claude 两次返回非 JSON 后按合同停用。两轮版本化修复后，repaired twin 最多只有 `5/8` 消除主审 blocking 问题，低于 `7/8` 推广门槛。这说明 LLM 适合作为语义问题发现者，但当前 legacy contract、teacher package 和 warning/blocking 裁决仍不稳定。最终为 `semantic_gate_hold_for_redesign`：未运行 5 题新生产、未审计剩余 60 题、未修改默认生产链。详细证据见 `docs/archive/phases/milestone_f2/MILESTONE_F2_SEMANTIC_VALIDITY_CALIBRATION.md`。

Milestone F3 已针对该根因完成生成器原生合同改造。语义合同不再由成品反向猜测，而是在 blueprint 后建立 design contract，参考材料生成后由 resolver 独立重算，再以 verified contract 绑定 rubric。现金对账不再要求未提供的期初/期末余额，三单匹配增加候选可见处置规则，费用题增加 `Attendee_Count`。离线校准得到旧8题 `8/8 blocked`、新8题 `8/8 verified + candidate-ready`，五题离线结构链为 `5/5`。真实旧8题仍为 `8/8` 非放行、严格 finding recall `94.7%`；但真实修复任务前三题仅1题 `pass_with_advisories`，另外2题 `needs_secondary_review`，且 Tuzi 随后因额度不足不可用。最大通过数已低于 `7/8` 门槛，因此状态为 `f3_semantic_gate_hold_for_redesign_and_secondary_capacity`，未运行后续扩展。

Milestone F3.1 解决了复审成本与三单匹配规则问题。备用 Tuzi key 的 `gpt-5.6-sol` 在 tracked public fixture 上完成两轮 blind+teacher，4/4 调用入账，最高输入/输出为 `949/542` tokens，成本约 ¥0.052，secret 扫描为0。新三单匹配合同明确两种数量差异、价格差异、重复业务键和状态优先级，重新生成的8题均 verified/candidate-ready。但第一题的 DeepSeek主审连续两次违反 requirement 覆盖合同，依照最多一次重试的规则停止，最终为 `f3_1_semantic_gate_hold`；没有运行五题 smoke或改变默认链。

Milestone F4 暂停扩展细粒度 semantic finding 流水线，转为整体任务编辑。固定8题由 `gpt-5.6-terra` 读取完整任务并生成版本化修订，程序从候选材料独立重算，`gpt-5.6-luna` 只看 candidate-visible 包独立求解。最终8/8通过、三个motif均至少2题通过、Luna关键结果8/8与重算一致、未解决重大歧义为0。Luna还发现“重复组所有成员是否均Hold”的残余规则问题，revision_02据此明确并通过。该阶段首次提出“LLM负责整体设计与编辑、程序负责真值和治理”的方向，但其路线比较建议已被后续 F4.3 证据和当前 R0–R7 重构计划取代。

Milestone F4.2 将该分工放入真正的 source→skill→task 新生产，而不是继续修旧题。8/8 新任务在最终 candidate 镜像下重新通过 truth、合同、视觉、盲解、人工放行和 export 复验。该阶段只证明“小规模 F4 混合闭环可行”；其内部通过结论随后被 F4.3 的真实交付失败证据重新限定，不能再作为路线比较或扩量的直接起点。

Milestone F4.3 随后对这8题执行四模型真实 rw-task 评测。32/32组合均尝试、无OOM，但复核发现旧汇总把“进程结束”误当成“有效交付”：实际仅15/32产生交付文件。Slots 1/3/4/6/7 均由 Sonnet、DeepSeek Pro 和 DeepSeek Flash 成功完成，有效输出平均分为 `0.98 / 0.96 / 1.00`；Sol 8题均未交付，其中1题最终超时。Slots 2/5/8 的四模型均未提交文件，根因分别是目标文件名冲突或模板提交位置不清。最终为 `f4_2_eval_reopens_task_quality`：先把 prompt、模板和 rw-task deliverable 路径一致性设为 blocking gate，再考虑路线对比；不扩题、不训练、不切换服务器 current。详细证据见 `docs/archive/phases/milestone_f4_3/MILESTONE_F4_3_MODEL_EVAL.md`。

下一主线已转为流水线重构与优化。当前问题不是单个模板缺陷，而是内部一致性尚未覆盖端到端行为有效性，同时生成与整体编辑为了保证可回答性而压低了任务复杂度，rubric 和 solver panel 也未形成可靠区分。统一问题定义见 `docs/architecture/pipeline_reconstruction_problem_statement.md`，活跃执行计划见 `docs/architecture/pipeline_reconstruction_optimization_plan.md`。

重构计划分为 R0–R7。当前 R0 已完成：Slots 2/5/8 的三类失败已固化为不含私有包、provider 输出或 secret 的 tracked fixtures。R1 的合同核心实现已完成：`DeliverableContract` schema/compiler/validator 已接入 whole-task materializer、通用 exporter、export validator、eval runner 和 F4.3 专用交付发现逻辑；缺少精确、非空、可打开的目标文件时不会进入 grader。离线 negative controls、正常 XLSX submission smoke、legacy compatibility run 和 `reconstruction_experimental` blocking profile 均通过，且无外部调用、canonical registry 哈希不变。由于本机 Docker Desktop Linux engine 不可用，使用受管服务器现有候选镜像作为固定 Linux/amd64 底座完成断网、只读、无凭据的源码快照复验；当前完整本地与固定 Linux/amd64 容器测试均为 249 项；最终容器指纹为 `0c1b370571db556be559ebf8615fb91197c7f7b9ac31ed6ac1dde6a12c73858a`，容器保持 `network=none`、只读 root、无 provider credentials，并在结束后删除临时路径和镜像。该证据只证明合同路径等价，不包含真实 solver 行为。

R2 的离线核心现已实现：`CapabilityBrief` 从 source refs、selected skills、typed resources、workflow roles、motif grammar 和 domain profile 生成；`TaskDesignProposal` schema、whole-context prompt、source/skill causal binding、capability behavior coverage、prompt-injection boundary 和 proposal-only executor 已完成。tracked valid proposal 的 source/skill/capability coverage 均为 100%，缺 provenance、装饰性 skill、未解决问题、越权提交路径和 prompt-injection echo 五类 negative controls 均被阻断。真实当前链在无 proposal 时明确记录 `awaiting_provider`，不会生成 proposal 文件，也不会把 brief/proposal 混入 package。executor 默认 `gpt-5.6-sol`，无显式授权不能外呼，并默认拒绝高价 `claude-sonnet-4-6`。

R2 的真实 provider 证据已从 v6 单 brief smoke 扩展到 v7 四 brief screening。v6 中 skill-guided 首次通过，LLM-led 首次因 causal binding 与 mutation authority 越界被阻断、再次调用后通过；后续 v7 证明当时的“重试”没有携带失败 proposal 或 validator finding，只是同提示重发，因此不能再称为受控 repair。当前合同已明确 `bound_element_ids` 只能精确引用 evidence node、relation、judgment 或 `deliverable:<exact section>`，并让唯一一次二次调用携带首轮 execution report、原 proposal、blocking findings、authority reasons 与可用 canonical IDs；实际发送的 repair prompt 也会留档。v16 已获得一条真实受控 repair 观测：policy-application 的 skill-guided proposal 首轮因两个未解决设计问题被阻断，第二轮明确引用首轮 execution report，并携带原 proposal、blocking finding 与 canonical IDs，随后 validation 和 materialization 均通过。该单点证据证明 repair 通路实际可用，但不证明跨 brief 稳定性、行为有效性或路线优越性。

R3 的离线 materializer 核心也已实现：tracked passing proposals 可生成 program-owned evidence dossier、candidate-visible XLSX 文件、从这些文件独立重算的 deterministic fact anchors、skill/capability-to-file/rubric bindings、`DeliverableContract` 编译 prompt、visual/openability report、candidate/teacher isolation 和 rw-task draft export。bounded repair loop 默认一次初始尝试加一次修订，并为 editor 编译实际 candidate package、只读 truth anchors、rubric bindings 和 governed contracts。versioned proposal-input manifest 已接入 Pipeline B、production runner 和 end-to-end CLI；它必须精确覆盖当前 batch case IDs，并校验 proposal SHA-256 和 brief identity。提供合格 proposal 时，reconstruction case 直接进入 hybrid materializer，不再同时生成 legacy prototype、teacher runner 和 rubric 链。

R4 的离线行为验证核心现已实现。solver tool preflight 明确检查 create/copy/edit/save/submit，混合路线在 preflight 缺失、失败或 solver model 不匹配时执行前阻断；运行报告区分 provider/tool failure、non-delivery、wrong path、invalid file、business error 和 professional-quality failure，并保存首次失败和 output fingerprints。grader 只有在 solver 进程成功且精确预期文件非空、可打开时才具备资格。确定性 reference smoke、mock execution matrix 和固定 Docker 完整代码路径均已通过；真实 Slots 2/5/8 与 solver/grader 证据仍未执行。

R5 的离线质量合同也已接入 hybrid materializer。每个任务输出五轴 `ValidityVector`、五维 productive-complexity `UtilityProfile`、selected-skill ablation、accidental-difficulty findings、七维 `RubricPlanV2` 和冻结的 calibration contract。rubric 将事实准确、证据追踪、方法过程、异常处置、可复核性、结构可用性和专业表达分开，事实权重为 0.20，每项具有独立 failure signal、证据要求和 0/0.5/1 部分得分语义。solver panel compiler 要求 weak/medium/strong 三层、模型唯一且全部通过同环境 preflight，并重新读取持久化 preflight 文件与嵌入报告逐字段比对；`BehavioralExecutionReport` 绑定 solver 实际读取的盲化候选包、输出树、preflight 与 delivery inspection 的指纹；grader stability analyzer 只接收有效交付，且 grader 输出文件必须存在并匹配声明的 SHA256；正式 evidence compile 需要先执行显式 freeze，并重新核对治理包、盲化包、三份行为报告、grader observations、专业复核及其支撑材料。四个 motif 的离线 R5 packages 均通过，但尚无真实 panel、grader repeat 或独立专家 observation，behavioral/professional 仍为 `not_evaluated`，因此 export 继续是 `draft_revise_only`、不可训练。

R6 已从 comparison scaffold 推进为受管 campaign。四个冻结 `CapabilityBrief` × strict-template、skill-guided LLM、LLM-led hybrid 三路线形成 12 个 route-blind assignments；三条路线比较的是设计权分配而非不同物化器：strict-template 由程序从 brief 编译，skill-guided 冻结角色、触发事件、工作流骨架和证据拓扑规模，LLM-led 可提出完整证据拓扑，三者共享确定性 materializer、真值、交付合同和 R5 门禁。campaign 固定 `gpt-5.6-sol` 作为两条 LLM 路线的设计模型，在 policy 与 executor 两层硬阻断 `claude-sonnet-4-6`，并实现最坏情况成本预留、assignment-scoped authorization、首错保留、分片 resume 和 12 包盲化 staging。结果侧不再人工拼表：证据编译器要求每题精确覆盖冻结三模型 panel，只允许 valid delivery 进入 grader，任何缺失、身份错配、替换 package root 或不完整 grader repeats 都阻断全部比较证据。

正式来源准入现已增加 `SourceProvenanceLedger` 与 `FormalBriefCohortAdmission`。Pipeline B selected-skill 接口会保留 registry 的 `source_candidate_ids` 和 `evidence_refs`，brief 必须把每个 skill 绑定到可解析的治理来源与 evidence spans；GDPVal、fixture、synthetic、opaque locator、未解析 source candidate、非 high workflow fit 或缺失角色均 fail closed。最初对 canonical registry/seed 的四组 occurrence 审计均为 0/4 admitted，证明不能直接使用混有早期 GDPVal 技能的旧 seed。

随后复用现有三组 public-source Pipeline A graph-calibration accepted candidates，在 ignored artifacts 中构建隔离 scratch registry（15 skills，全部保留 typed resources 和 source evidence），没有修改 canonical registry/readiness。第一版四 briefs 因跨三个业务 workflow group 被 4/4 blocked；按工作流组重采样的新 cohort 为 4/4 admitted。`r6_formal_public_v6` 冻结为只读 smoke 证据。`r6_formal_public_v7` 的四份 assignment-scoped 授权现已执行完毕：只使用 `gpt-5.6-sol`，8 个 LLM assignments 共发生 13 次 provider 调用，4 个物化、4 个在两次调用后阻断；skill-guided 为 1/4，LLM-led 为 3/4，严格模板为 4/4。总计 115,835 tokens、1041.827 秒，按治理上限预留 26/32 美元；raw response、solver、grader、专业复核、registry/release/promotion 均未发生。provider-screening outcome 为 `redesign_again`，原因是 package completion 不完整、两条 provider route 跨 brief 不稳定、5 次二次调用均未携带失败反馈，以及 `bound_element_ids` 引用合同表达不足。该结果不是行为路线比较，不能据 1/4 与 3/4 宣称路线优越。

R7 的 report-only promotion 核心已建立并改为 artifact-bound。screening report 必须绑定 comparison manifest 与完整 12-record evidence compile report，并由程序重新计算路线聚合、绝对门槛和 confirmation candidates；confirmation 不再接受一组自报布尔值，而是由 absolute gates、matched environment、server candidate reproduction、rollback verification、major-validity closure 五类独立 gate reports 编译，每份 gate 及其支撑文件均绑定 SHA256。缺失、篡改、环境不一致或重大 finding 未关闭时输出 `hold`/`redesign_again`。即使完整 confirmation 输出 `promote`，也只代表 opt-in candidate profile 资格，默认链、release、registry 和 training authority 仍全部为 false。

production readiness 已增加 design gate：只要启用 design frontend，必须同时满足 proposal validation pass 与 hybrid materialization pass；`awaiting_provider` 下 legacy package 即使内部仍显示 `candidate_ready`，production summary 也只能停在 `verifier_passed`，不得进入 candidate-ready、production eligibility 或 training pool。validated proposal 已通过 exact-coverage、SHA 和 brief identity 校验的 manifest 正式接入 batch route，命中后直接进入 hybrid materializer。受管执行顺序、停止规则、solver/grader evidence、服务器清理与 rollback 已同步到 `docs/operations/pipeline_reconstruction_runbook.md`。v15 的最小 cross-check slice 已在用户授权下完成：`brief_31f83336b665` 的 skill-guided 与 LLM-led 两条路线均在首轮通过 proposal validation 和 hybrid materialization，共 2 次 `gpt-5.6-sol` 调用、19,851 tokens、173.937 秒、4/8 美元预留；Claude、solver、grader、专业复核和 mutation 均未使用。该 brief 在 v7 两条路线都 blocked，因此结果支持新版 initial prompt/canonical namespace 的直接改善，但没有触发 repair，也不支持路线优越性。执行后审计修正了 preflight 外部调用字段和 partial-screening 语义，v15 因代码指纹变化 fail closed 并保留为 evidence。新权威候选 `r6_formal_public_v16` 的 policy-application 最小双路线申请已获用户授权并完成：`brief_93974f826477` 的 LLM-led 首轮通过并物化；skill-guided 首轮因两个 unresolved design questions 被阻断，保留首错后由 feedback-conditioned repair 在第二轮通过并物化。共 3 次 `gpt-5.6-sol` 调用、31,026 tokens、226.827 秒、6/8 美元预留；Claude、solver、grader、专业复核和 mutation 均未使用。`v3.provider_screening_outcome.2` 为 `awaiting_provider_completion`，当前 2/8 provider assignments materialized、0 blocked、1 次真实反馈修复、0 次无条件重试；这仍是局部 provider/materialization 证据，不得据此启动 solver/grader 或判断路线优胜。

随后对 v15/v16 两个 brief 的三路线 candidate workbooks 做了 artifact-tool 只读内容与视觉审计。58 个 XLSX 全部可打开、可渲染且与 rw-task export 副本逐字节一致，但每个文件平均仅约 4.2 行 × 5 列、约 21 个非空单元格，合计公式数为 0；无论文件声称 materiality threshold、ownership/frequency、walkthrough、exception/remediation 还是 synthesis，实际都复用 `Evidence_ID / Record_Key / Observed_Value / Observation_Status / Source_Ref` 五列与 100/200/300 类占位数值。渲染还显示长表头和 ID 被默认列宽截断，而原 visual gate 仍判 pass。新建 report-first `v3.evidence_content_quality.1` 后，六个 route packages、58/58 文件全部 blocked；v16 LLM-led 另命中 `candidate_output_modeled_as_reference_input`。因此 R3 被重新打开：现有 factual pass 仅表示占位事实可被同一程序重算，不能表示业务事实正确或真实；v16 冻结为诊断证据，不再追加 provider。下一步必须先引入结构化 `EvidenceArtifactSpec`、方法来源/场景事实分离、语义内容与真实渲染 blocking gates，并让 Validity 区分 `recomputable` 与 `business_grounded`。

R3/R5 的本地 V2 修复现已落地。`TaskDesignProposal` 支持 `v3.task_design_proposal.2`：每个 candidate-visible evidence node 必须提供 typed `v3.evidence_artifact_spec.1`、至少四个业务字段、至少三条 coherent records、稳定 primary key、明确 `fact_origin` 与 methodological refs；每条 evidence relation 必须声明 join fields 和可选 field comparisons。provider prompt 已改为要求 V2，真实 provider 返回 V1 会在 proposal-only executor 中 fail closed；候选人应产出的 synthesis 不允许再作为 reference evidence。Hybrid materializer 对 V2 按声明字段与类型写入业务表、设置可读列宽和数值/日期格式，并增加 `Provenance` sheet 明示“方法来源”与“经治理虚构场景事实”的边界；fact anchors 从实际文件按 join/comparison contract 重算。`v3.evidence_content_quality.1` 对 V2 已取得 blocking authority，旧 generic display schema 会使 materialization blocked。strict-template 进一步改为 motif-aware：fan-in reconciliation、cross-check validation、policy application 与 evidence-to-deliverable 分别使用交易/金额、断言/独立结果、控制/阈值、finding/风险暴露字段和各自 join/comparison contract，不再共享一张弱化的通用表；artifact-tool 对四类 Evidence/Provenance 页的 16 个 XLSX、32 个 sheet 完成实际渲染，未见裁切，且全部与 rw-task export 副本逐字节一致。Validity 现将 candidate-visible deterministic recomputation 降为 `factual_validity=provisional`，generator-owned evidence 不再自行升级为 business-grounded pass。离线测量同时发现 V2 strict proposal 压缩后约需 8.4k–13.1k tokens，旧 6k completion 上限必然存在截断风险；新 policy/executor 因此把 V2 provider 下限设为 16k tokens，而每次 2 美元和 campaign 32 美元费用天花板保持不变。v17/v19 分别因预算不足和后续基线改进而冻结；新的正式 campaign 必须在最终同步指纹下重建，并重新取得 exact provider authorization。

v21 已在会话授权下完成 policy-application 的最小 V2 provider slice。skill-guided 与 LLM-led 均在首轮通过 proposal validation 和 semantic materialization，只使用 `gpt-5.6-sol`：共 2 次调用、26,806 tokens、287.297 秒、4/8 美元预留；分别生成 11 个/4 个语义 evidence artifacts 和 17 个/8 个 deterministic anchors，未调用 Claude、solver、grader，也未发生 mutation。对历史、strict V2、v21 原始包及离线重物化包的 artifact-tool 审计覆盖 104 个工作簿、150 张 sheet 预览，104/104 rw-task export 副本逐字节一致。审计确认 v21 内容已摆脱五列占位表，但暴露了长文本未换行以及 provider 使用 `0/1`、snake_case 状态值的显示质量问题；materializer 已增加长文本换行和自适应行高，离线重物化实图复验通过，proposal prompt 也已要求可读业务标签和 `Yes/No` 枚举。冻结的 v21 原提案不做事后改写，仍作为诊断证据；新 prompt 的真实效果必须由新鲜 provider slice 验证。该阶段完整本地回归为 230/230，Validity/Utility 仍为 `provisional`，因此 v21 不授权 solver/grader、训练或路线优越性结论。

v22 随后在同一 policy-application brief 上验证新 prompt。skill-guided 首轮通过并物化 11 个工作簿，实际渲染确认 `Yes/No`、`Per change`、`Pending assessment` 等均为业务可读值；扩展审计累计覆盖 115 个工作簿、172 张预览，115/115 export 副本一致。LLM-led 首轮返回完整响应但未通过 V2 schema 解析，状态为 `contract_failed`，无可反馈修复的已解析 proposal。该历史失败报告发生在 sanitized schema findings 功能加入之前，因此只记录 `ValidationError`，没有 findings 明细。当前代码已收紧为：只有 `proposal_blocked` 且保存了 proposal 的首错才可进入唯一一次 feedback-conditioned repair；未来 contract failure 会保存不含 provider 字段值的 schema location/type/message 后停止，不再预留或发起无条件重试。v22 共 2 次 `gpt-5.6-sol` 调用、预留 4/8 美元，未使用 Claude、solver、grader 或 mutation；其部分结果继续是诊断证据。

R2.1/R3.1 schema-interface hardening 的离线核心现已完成。provider 默认面对较小的 `v3.task_design_semantic_proposal.1`，继续完整负责场景、业务字段、记录值、证据关系、判断、skill bindings、交付意图与 productive complexity；程序只允许把 flat records 包装为严格 `v3.task_design_proposal.2`，并写入版本、`candidate_visible=true`、`proposal_origin=llm` 和全 false authority。normalization report 对 provider-owned semantics 计算前后哈希，要求 `facts_added=0`、`facts_removed=0`、productive complexity 完整保留；缺主键、未知字段、旧 nested record、错误 union、boolean/机器化 enum、超长 payload、semantic loss 与 authority 越界均 fail closed，sanitized findings 不包含 provider 字段值。executor 仍兼容直接 strict V2，但只有 `proposal_blocked` 且已保存 strict proposal 才能 repair；contract/provider failure 在 executor 层也直接停止。该接口加固阶段的历史快照为 245/245、`a437172f...0ffd`；后续 typed-record 修复后的当前基线为 249/249、`0c1b3705...858a`。

v15、v16、v21、v22 均已冻结为不同问题层的诊断证据；任何旧 receipt、旧命令或剩余 assignment 都不得复用。新的 `r6_formal_public_v23` 在当前代码下完成了首个 semantic-interface exact pair：用户授权 request `8b285191...927e` 后，skill-guided 与 LLM-led 均在首轮 `gpt-5.6-sol` 调用中通过 semantic schema、无损 normalization、proposal validation 和 hybrid materialization，无 repair。两次调用合计 25,721 tokens、281.86 秒，预算预留 4/8 美元；normalization 两路线均为事实增删 0、provider-owned semantic hashes 相等、productive complexity 保留、authority 由程序写入且全 false。skill-guided 生成 11 个工作簿/17 anchors，LLM-led 生成 5 个工作簿/10 anchors；16/16 candidate/export 副本逐字节一致。artifact-tool 对同 brief 三路线共 27 个工作簿、54 张 Evidence/Provenance sheet 全部实际导入、渲染并逐张检查，0 失败、0 公式错误、provider Evidence 中 0 个 machine-facing label 命中。Validity/Utility 仍为 provisional，partial screening 为 `awaiting_provider_completion`；Claude、solver、grader、专业复核和所有 mutation 均未使用。该单 brief pair 证明新接口可以被两条路线遵循，但不证明跨 brief 稳定性、行为有效性、路线优越性或训练价值。

v24 cross-check exact request `8761b514...0f57` 获授权后完成两次 `gpt-5.6-sol` 首轮调用，共 28,192 tokens、294.265 秒并预留 4/8 美元。两路线 semantic normalization 都是事实增删 0、语义哈希相等、复杂度保留和零 authority；LLM-led 通过物化，skill-guided 在内容 gate 被阻断。30 个工作簿、60 张 Evidence/Provenance 实图审计均可渲染、无公式错误且 candidate/export 逐字节一致。进一步审计确认 skill 阻断来自 `not candidate-authored` 否定句的误报，同时 12 个 Provenance sheet 因 artifact-level source projection 为空而缺少方法来源。当前代码已加入否定语境处理，并强制每个 artifact 的 `methodological_source_ref_ids` 与 node `source_ref_ids` 精确相等；该差异在 proposal validation 层阻断并允许唯一一次 feedback repair。

为避免为了新合同人工伪造失败报告，现增加 persisted strict V2 proposal 的确定性离线 replay：它用当前 prompt、validation、binding 和 authority 重新编译证据，固定 `allow_external_provider=false`，不产生 provider diagnostics，也不能物化或 mutation。v24 保存的 skill proposal 已据此形成 1/1 portable repair-readiness bundle，并通过独立 replay 校验。fresh `r6_formal_public_v25` 在最终指纹下通过 formal admission、四个 motif-aware strict controls 与 structural preflight。

v25 cross-check exact request `023cb1df...62da` 获授权后完成：skill-guided 首轮通过并物化；LLM-led 首轮仅因一个冻结 source 未进入 evidence topology 而 `proposal_blocked`，保存首错后由 feedback-conditioned repair 通过并物化。三次 `gpt-5.6-sol` 调用共 52,513 tokens、446.531 秒，预留 6/8 美元；无 unconditioned retry。两条最终 proposal 的 semantic hash 均保持一致、事实增删 0/0、productive complexity 保留、node/artifact provenance 精确相等，生成 12/7 个工作簿与 21/14 个 anchors。artifact-tool 对同 brief 三路线共导入 31 个工作簿、渲染并逐张检查 62 张 Evidence/Provenance sheet：0 失败、0 公式错误、Evidence 中 0 个机器化标签、31/31 Provenance 来源非空，31/31 candidate/export 副本逐字节一致。Validity/Utility 仍为 provisional，screening 仍为 `awaiting_provider_completion`（2/8 provider assignments materialized），不能据此声称行为有效、路线优胜或 training-ready。

v25 fan-in reconciliation exact request `a873d10e...e6f98` 获授权后也完成：skill-guided 与 LLM-led 均首轮通过并物化，无 repair。两次 `gpt-5.6-sol` 调用合计 23,926 tokens、253.187 秒并预留 4/8 美元；分别生成 8/5 个工作簿与 12/8 个 anchors。两条 proposal 的 semantic hash 均保持一致、事实增删 0/0、productive complexity 保留、四个冻结 source 精确投影且 authority 由程序写入为零。artifact-tool 对 matched 三路线共导入 21 个工作簿并逐张检查 42 张 Evidence/Provenance sheet：0 失败、0 公式错误、Evidence 中 0 个机器化标签、21/21 provenance 非空，21/21 candidate/export 副本逐字节一致。该第二 brief 使同一指纹下的 provider conformance 扩展到跨 brief，但仍不构成行为有效性或路线优胜证据。

v25 policy-application exact request `fb3c00c...f084` 获授权后完成两次 `gpt-5.6-sol` 首轮调用，共 24,303 tokens、255.220 秒并预留 4/8 美元。两条 provider proposal 都通过 semantic schema、无损 normalization、proposal validation、source/skill binding 与零 authority gate；LLM-led 随后物化通过，skill-guided 则在 deterministic content gate 被冻结为 `hybrid_materialization_blocked`。合同不允许对 materialization failure 再调用 provider，因此没有第二次尝试。

审计确认 skill-guided 阻断是 validator 的单复数 token 假阳性：artifact role 使用 `thresholds/metrics`，工作簿表头使用 `Threshold/Metric`，但字段、记录和来源均完整。当前 validator 已加入保守的常见复数归一化并保留 unrelated-role negative control；零外呼 content replay 使 11/11 skill-guided workbooks 通过，原 V25 block 仍作为冻结首错保留。artifact-tool 对 policy matched 三路线共导入 26 个工作簿并逐张检查 52 张 Evidence/Provenance sheet：0 失败、0 公式错误、Evidence 中 0 个机器化标签、26/26 provenance 非空，26/26 candidate/export 副本逐字节一致。

该 V25 修复快照的完整本地与固定 Linux/amd64 容器测试均为 245/245，源码指纹 `a437172ffbf9328a0a0edc6936aaacca981ad88d4eb8f968ce6cb0f673aa0ffd`；容器断网、只读、无 provider credentials，且临时目录和镜像清理成功。V25 因代码指纹变化冻结，最终 screening 为 `awaiting_provider_completion`：5/8 provider assignments materialized、1 blocked、2 个 experimental assignments 未运行，7 calls、1 feedback retry、0 unconditioned retry、100,742 tokens、954.938 秒、14 USD reserved。不得继续 V25 的 `evidence_to_deliverable`、复用旧 receipt 或从该证据启动 solver/grader。任何下一 external/provider/behavioral slice 都必须重新同步指纹并取得 exact authorization；Claude、solver、grader、专业复核与 mutation 仍未使用。

fresh `r6_formal_public_v26` 完成两个获授权 slice。policy exact request `7457b1f7...57dc` 的 skill-guided 与 LLM-led 均首轮物化，合计 27,194 tokens、303.000 秒、4/8 美元；16 个工作簿与 32 张 Evidence/Provenance sheet 全部通过实审。fan-in exact request `79559adc...60da` 中，skill-guided proposal 首轮通过但在 deterministic content gate 被冻结；LLM-led 首轮因一个冻结 source 未进入 topology 而 `proposal_blocked`，随后 feedback-conditioned repair 通过并物化。fan-in 三次调用共 38,247 tokens、310.297 秒、6/8 美元，0 unconditioned retry；两路线 8/4 个工作簿、24 张 sheet 全部可导入、可读、无公式错误、Evidence 无机器化标签、12/12 provenance 非空且 12/12 candidate/export 副本一致。

fan-in skill block 经实图与字段审计确认是 `criteria` 与 `Criterion` 的不规则单复数假阳性，而不是内容缺失。validator 已加入保守的不规则词形映射，并保留 role-unrelated negative control；针对性测试 13/13，冻结 proposal/files 的零外呼 replay 从 7/8 恢复为 8/8。V26 因代码指纹变化冻结，screening 为 `awaiting_provider_completion`：3/8 materialized、1 blocked、4 pending、5 calls、1 feedback retry、65,441 tokens、613.297 秒、10 USD reserved。

`r6_formal_public_v27` fan-in exact request `f187ccd3...45fc` 已获授权并消费。LLM-led 首轮 proposal/materialization 通过；skill-guided proposal 首轮通过，但 deterministic content gate 因未使用 typed `record_type=evidence_assessment_criterion` 而把语义明确的 Criterion 表误判为 role/header 不匹配。两次 `gpt-5.6-sol` 调用共 23,976 tokens、284.375 秒、4/8 美元，未发生 retry。两路线 13 个工作簿、26 张 Evidence/Provenance sheet 全部通过导入、逐图可读性、公式、可读标签、provenance 和 13/13 candidate/export 字节一致性审计。

validator 现把 provider-owned `record_type` 纳入 role-side semantic tokens，但不纳入 field display names，避免门禁自证式通过；正例与 unrelated-record negative control 均通过，冻结 proposal/files 的零外呼 replay 为 8/8。完整本地与固定 Linux/amd64 容器均为 249/249，当前源码指纹 `0c1b370571db556be559ebf8615fb91197c7f7b9ac31ed6ac1dde6a12c73858a`；容器断网、只读、无 provider credentials 且清理成功。V27 因后置代码变化冻结，正式 screening 写入按预期因 fingerprint mismatch fail closed。

fresh `r6_formal_public_v28` 已在当前指纹下完成四个获授权 sequential slices。前两个 requests 的四条路线均首轮物化，54,527 tokens、586.703 秒。第三个 request `2238fd1f...c09d8` 中，LLM-led 首轮通过并物化；skill-guided 因关系 `relation_walkthrough_to_deficiency_policy` 缺少必需 join contract 而在 semantic normalization 阶段 `contract_failed`，没有 semantic/strict proposal 落盘，因此不具 feedback repair 资格并在一次调用后停止。第三批两次调用共 24,090 tokens、245.750 秒。

最后 exact request `3b563b61...c4f3` 已获授权并由 receipt `a5afbbfd...a922` 绑定；brief `brief_b9bf5ab545c1` 的 skill-guided `cmp_af4702186df55d37` 与 LLM-led `cmp_345146229dec7eab` 均首轮 proposal/materialization pass，未发生 retry。V28 合计 8 次 `gpt-5.6-sol` 调用、102,082 tokens、1,076.593 秒和 16 USD 预留上限；`claude-sonnet-4-6`、solver、grader、专业复核与全部 mutation 均未使用。7 个已物化 provider assignments 合计 47 个 provider-route 工作簿、94 张 Evidence/Provenance sheet，经 artifact-tool 导入、公式和逐图审计均无内容/显示失败，47/47 provenance 完整且 47/47 candidate/export 字节一致。完整本地回归为 249/249。

V28 正式 provider screening=`redesign_again`：strict control 4/4、LLM-led 4/4、skill-guided 3/4，reason codes 为 `provider_package_completion_below_contract` 与 `provider_route_cross_brief_instability`。已冻结首错不得被覆盖，matched packages 不完整，因此禁止进入 solver/grader、路线优越性判断或 promotion；下一 campaign 必须在离线修复并重新同步指纹后重新冻结和授权。

V28 closeout 后的 join-contract 重构已离线完成。`v3.task_design_semantic_proposal.1` 现在把每条 relation 的 `from_join_field` 与 `to_join_field` 设为非空 schema 必填项，route prompts 明确禁止省略或使用 `null`；normalizer 仍不允许程序代填业务 join key。若兼容 provider 返回整体可解析但 join contract 不完整的 semantic draft，executor 会保存 draft、sanitized normalization findings 和实际初始 prompt，并标记 `semantic_proposal_blocked`；唯一第二次调用必须携带原 draft、精确 findings 与 canonical IDs，screening 通过初始/修复 prompt 实际文件判定 feedback-conditioned。广泛 schema-invalid、provider failure、materialization failure 与无持久化 draft 的失败仍不可重试。tracked negative fixture、normalizer/executor/campaign 正负回归 42/42、完整本地与固定 Linux/amd64 容器 252/252 均通过；容器保持断网、只读、无凭据并清理成功，parity 指纹 `2178bd38...c23b`，campaign code fingerprint `4ec4dadb...b36b`。V29 当时在该指纹下完成四个 strict controls、portable repair-readiness 1/1、structural preflight 与 exact request `df63258c1258b91a10d7d152d21989ca9867a32608d900e89bb03bb052b1bf1d`；随后获授权的执行结果见下段。

V29 exact request `df63258c...bf1d` 已获授权并由 receipt `89c40027...a40d` 绑定。skill-guided `cmp_2d7e813026db6bc2` 首轮 proposal/materialization pass；LLM-led `cmp_996bef081a8fa015` 首轮因同一 skill 出现重复 binding 而 `proposal_blocked`，保存首错、proposal、validation、normalization 和实际 prompt 后，唯一一次 feedback-conditioned repair 通过并物化。总计 3 次 `gpt-5.6-sol` 调用、44,270 tokens、377.390 秒、6/8 USD 预留，0 unconditioned retry；Claude、solver、grader、专业复核和 mutation 均未使用。两条 provider 路线的 16 个候选工作簿、32 张 Evidence/Provenance sheet 经 artifact-tool 实际导入和逐图审计，0 公式错误、0 空白/裁切/机器标签问题，16/16 provenance 非空且 16/16 candidate/export 字节一致。V29 screening 冻结为 `redesign_again`，但历史 reason code 将所有 skill-binding 失败粗略归为 bound-element namespace；不得用新代码重写该报告。

V29 后离线修复明确要求每个 selected skill 恰好一个 `skill_bindings` entry，并把 namespace、cardinality、missing/decorative 三类失败分别分类；新增回归后本地与固定 Linux/amd64 容器均为 253/253，container parity `989dd8bb...4fe`，campaign code fingerprint `c0860676...4990`。V29 duplicate-binding 首错的 portable repair-readiness 1/1 pass。V30 `r6_formal_public_v30` 随后冻结四个 strict controls并通过 structural preflight；immutable exact request `32573db4b13f492890949495656c4b7a4d48edbd1e48def339d59a4de7c5dcf1` 仅覆盖同一 brief 的 skill-guided `cmp_9a365f866100f17c` 与 LLM-led `cmp_ebf56b985922a6cc`、`gpt-5.6-sol` 和最多 8 USD，排除 solver/grader/review/mutation；此处记录的是调用前快照。

V30 首个 request `32573db4...dcf1` 已由 receipt `66d011a2...fe95` 消费。skill-guided 首轮 proposal/materialization pass；LLM-led 首轮因未投影 `source_candidate_8ab01be5c2` 而保存 `proposal_blocked` 首错，唯一 feedback-conditioned repair 通过并物化。3 次 `gpt-5.6-sol` 调用共 42,616 tokens、354.235 秒、6/8 USD，0 unconditioned retry；15 个 provider-route XLSX/30 sheets 的实际导入、渲染、公式、provenance 与 export identity 审计全部通过。partial screening=`awaiting_provider_completion`，仅有 `provider_campaign_incomplete`，说明 cardinality 修复没有引入新的 blocking contract reason。下一 immutable exact request `74a6e65ff0eb93d85f161a8cda7928092be2005471693bea180259f67d125d47` 仅覆盖 brief `brief_31f83336b665` 的 skill-guided `cmp_6e163b9e43cda1d2` 与 LLM-led `cmp_a8ea6655de2a674c`、`gpt-5.6-sol` 和最多 8 USD；此处记录的是用户确认前快照，后续审计结论见下段。

用户随后授权 `74a6e65f...5d47`，但 receipt 编译前的操作审计发现：若把上一 slice 的有效旧 receipt 交给 preflight，它会在未校验当前 active request SHA 的情况下把 campaign 标成 `provider_generation_ready`；scope gate 虽会在真正生成时拒绝新 task IDs，但状态本身已错误。该授权没有被消费，0 新 provider calls。当前 preflight 在 active request 存在时必须要求 receipt 的 `authorization_request_sha256` 精确等于 active request 内容 SHA；旧 receipt 现在 fail closed 为 `authorization_receipt_active_request_mismatch`，不得改变 campaign 状态。新增 sequential regression 后 local/container 254/254，parity `b31a79b3...505d`，code fingerprint `b28556f0...b392`。

V31 `r6_formal_public_v31` 的 cross-check request `6e486b9d...0ba5` 已由 receipt `5f0b9c6d...7aef4` 消费。LLM-led 首轮 proposal/materialization pass；skill-guided 首轮因 mutation-authority 越界保存 `proposal_blocked` 首错，随后唯一 feedback-conditioned repair 移除越权并通过物化。3 次 `gpt-5.6-sol` 调用共 58,916 tokens、501.297 秒、6/8 USD，0 unconditioned retry。两路线 18 个工作簿/36 张 Evidence-Provenance sheet 全部经 artifact-tool 导入、渲染和逐图审计，0 公式/显示错误、provenance 完整、18/18 candidate/export 字节一致。partial screening=`awaiting_provider_completion`，唯一 reason 为 `provider_campaign_incomplete`；这仍不是行为或训练证据。

V31 下一 immutable exact request `5ad5bb68c70b307f57cf4a66bb70228e22236683f318a8735a66600b160546cf` 仅覆盖 policy-application brief `brief_93974f826477` 的 skill-guided `cmp_000f7ca3889507c9` 与 LLM-led `cmp_f82bff62a06e402e`、`gpt-5.6-sol` 和最多 8 USD，排除 solver/grader/review/registry/release/promotion；external calls=0，等待新的 exact 授权。

该 policy-application request 已由 receipt `fb194651...e55b8` 消费，两条路线均首轮 proposal/materialization pass。2 次 `gpt-5.6-sol` 调用共 24,923 tokens、269.062 秒、4/8 USD，无 repair。两路线 15 个工作簿/30 张 sheet 全部经 artifact-tool 导入、渲染和逐图审计，0 公式/显示错误、provenance 完整、15/15 candidate/export 字节一致；content、visual、materialization 均 pass，rw-task export 为 0 blocking 的 `draft_compatible`。V31 累计 4/8 materialized、5 calls、83,839 tokens、770.359 秒、10 USD，partial screening 唯一 reason 仍为 `provider_campaign_incomplete`。

V31 下一 immutable exact request `34b32317261856d0ceb948cb587cd2930fd08bb9701bb4b83bf6daafcdcc77c0` 仅覆盖 fan-in brief `brief_2630d8f77264` 的 skill-guided `cmp_5033d781118d65e2` 与 LLM-led `cmp_34da237cf93dcc20`、`gpt-5.6-sol` 和最多 8 USD，排除 solver/grader/review/registry/release/promotion；external calls=0，等待新的 exact 授权。

该 fan-in request 已由 receipt `4755fcec...7f2d0` 消费，两条路线均首轮 proposal/materialization pass。2 次 `gpt-5.6-sol` 调用共 24,060 tokens、253.124 秒、4/8 USD，无 repair。两路线 13 个工作簿/26 张 sheet 全部经 artifact-tool 导入、渲染和逐图审计，0 公式/显示错误、provenance 完整、13/13 candidate/export 字节一致；content、visual、materialization 均 pass，rw-task export 为 0 blocking 的 `draft_compatible`。V31 累计 6/8 materialized、7 calls、107,899 tokens、1023.483 秒、14 USD，partial screening 唯一 reason 仍为 `provider_campaign_incomplete`。

V31 最后一对 experimental `evidence_to_deliverable` 已由 exact request `588d7708f7389622166c51bbae7b63af53077227b7bad2b514e045d36890af3f` 授权并消费。skill-guided 首轮通过并物化；LLM-led 首轮因未使用冻结来源而保留 `proposal_blocked`，随后唯一 feedback-conditioned repair 通过并物化。该 pair 共 3 calls、40,810 tokens、387.391 秒、6 USD；12 个工作簿/24 张 sheet 的 artifact-tool 导入、公式、显示与 provenance 审计通过，12/12 candidate/export 字节一致。V31 累计 8/8 materialized、10 calls、148,709 tokens、1410.874 秒、20 USD，formal provider screening=`proceed_to_behavioral_evaluation`。随后 12/12 route-blind staging pass，report SHA=`a1c014a5...928d3`，不含 teacher artifacts，campaign=`evaluation_ready`。这只关闭 provider/materialization 门，不提升 experimental motif，也不授权 solver、grader、专业复核或 promotion。

在完成路线比较和 promotion 判断前，不继续批量扩题、不进入 RL/SFT、不推广 `evidence_to_deliverable`、不修改 canonical registry，也不切换服务器 `current` release。

2026-07-19 最新执行状态：Track A 离线协议观测、连续两次空响应快速失败和 context/completion 预算拆分已经实现；Track B 六题现实任务独立审查契约已经实现。完整本地与固定 Linux/amd64 均通过 292/292。尚未进行新的 provider call；下一步只允许编译并申请一个 `gpt-5.6-sol` 公开 fixture、单次 attempt、最多两次 provider calls 的精确授权。

首份协议探针 exact request `993d31a4...f37a4` 已获授权，但执行器在进入 `subprocess.run` 前因新请求的 `timeout_seconds` 与旧执行路径的 `timeout_seconds_per_model` 字段不兼容而本地失败；零 provider calls。该 receipt 已按 fail-closed 规则消费并冻结，禁止重用。字段适配已修复并加入回归测试；后续必须重新完成最终指纹、parity 和 exact authorization。

修复后的 exact request `ea3afe62...aae45` 已获授权并完成唯一公开协议探针。`gpt-5.6-sol` 在两次调用中均有正常 provider token usage，但映射后的 assistant 消息均无 content、tool calls 或 reasoning，零 sandbox/tool entry、零交付物；执行器按规则在第二次空响应后停止。实际使用 4,952 input tokens、1,109 output tokens，合同成本 0.08279 USD。证据完整性复核通过。结论是当前 Tuzi Chat Completions → Stirrup agent 链路不兼容，不是 `gpt-5.6-sol` 能力失败；适配工作达到预设上限，不再追加第三轮探针。任务设计 provider 路径中的 `gpt-5.6-sol` 历史证据不受此 solver-agent 结论推翻。

Reality cohort 已切换为官方 `deepseek-v4-pro` 独立 LLM proxy，并从头审查六个 route-blind 包，不混用 Gemini 结果。每题分 candidate-visible reality 与 teacher-rubric focus 两阶段，固定六题 candidate 后六题 rubric；12 个有效阶段、每阶段至多两次尝试、最多 24 calls、0.25 USD 总上限。只有 timeout/408/429/5xx、空响应、截断、非法 JSON 或 schema 缺失允许一次保留首错的格式补跑；实质结论不得重抽。LLM 结果最多授权进入受限 screening，专业有效性保持 provisional，不能作为 expert、training 或 promotion 证据。任何外呼必须等待新的 immutable exact request SHA 授权。

首份 Reality LLM exact request `1dfe222e...32916` 已获授权，但真实 XLSX payload 中的 `datetime` 在进入 provider client 前触发 JSON 序列化错误；manifest 冻结为 `incomplete`、`completed_calls=0`，未上传包且零 Gemini calls。该 receipt 禁止重用。序列化现统一使用安全字符串转换，并把 candidate tree、package fingerprint 与 rubric hash 的复核移入执行器，修复后必须重新完成 fingerprint、parity 和 exact authorization。

第二份 Reality request `2cff1fa3...ceae9` 已消费：前两个 candidate-blind reviews 解析成功且五维均 pass；第三个 provider response 未满足 JSON 合同，campaign 在 3 次 provider calls、2 次 completed reviews 后冻结为 `incomplete`，rubric 阶段未开始。两份 Gemini pass 仅保留为历史 provider 诊断，不进入新的 DeepSeek cohort。未执行的 Gemini continuation request `76c34304...d554` 已因模型切换废弃，不得创建 receipt。项目级策略同时禁止 Tuzi `claude-*` 和 `gpt-5.4-pro`，除非用户未来明确撤销。

官方 DeepSeek V4 Pro Reality Cohort exact request `8d04b2c0...acee0` 已获授权并完成：12/12 阶段均首轮通过 JSON/schema 合同，12 provider calls、0 controlled retry、73,776 prompt tokens、26,943 completion tokens、420.531 provider seconds。六题 candidate-blind 五维全部 pass；五题 rubric pass，`cmp_34da237cf93dcc20` 的 fan-in rubric 因多个 criteria 共享 skill/evidence 要求、存在重复计分风险而 revise。cohort=`revise_before_screening`、`screening_eligible=false`，无 blocked；不得启动 12 题 screening。专业有效性仍为 provisional，未授权 solver、grader、expert、training 或 promotion。

随后 report-first 横向审计发现六题 rubric 的计分语义完全同型：每题 7 个最终 criteria、21 个 pair，Utility criteria 均共享 brief-level skill/capability 元数据，但 observable behavior 与 independent failure signal 均唯一；binding 明确为 `final_weights_assigned=false`/`program_compiled_binding_not_final_weight`。强化后的 `RubricScoringAuthorityAudit` 对六题全部 pass，共同结构签名为 `0e4bbcae...43ca9`，共同 scoring-semantic signature 为 `1c4ffda4...b9525ec`。Rubric Focus V3 现强制完整 pairwise 判断、与 deterministic shared-evidence 分类一致，并恢复旧 V2 入口兼容；本地完整套件 313/313 通过。该 workstream 的强制边界是：最终 Linux/amd64 parity 必须绑定文档同步后的 source fingerprint，随后只能编译 immutable exact request 并等待用户确认 SHA；在此之前及同一步均不得创建 receipt 或调用 provider。后续只复用原 candidate pass，最多 12 DeepSeek calls/0.13 USD。

Rubric recalibration exact request `63a9b799...e0897` 经用户知情授权后已消费并冻结为 `incomplete`。共执行 3 次官方 DeepSeek calls、使用 1 次受控补跑、成本预留 0.03 USD：`cmp_000f7ca3889507c9` 首轮完成并 pass；`cmp_3146d2f399790f96` 的首轮和唯一补跑均在 4,000 completion tokens 处以 `finish_reason=length` 截断，未形成可解析结论。其余四题未调用。manifest SHA 为 `656c1d50...ead31`；该 receipt 不得重用，也不得追加第三次尝试。当前瓶颈是 21-pair V3 输出合同超过 4k completion ceiling，不是新的 rubric revise/blocked 证据；cohort 继续为 `incomplete`，screening 仍未授权。

针对该瓶颈，离线 Compact V4 已实现且保留 V1/V3 读取兼容：21 个 pair 仍须全部返回，但非风险 pair 只输出 criterion IDs 与分类；详细 rationale/evidence 仅允许出现在 `potential_duplicate/duplicate` 的 `risk_findings`。六题真实完整输入估算为 8,131–10,114 tokens，标准全 pass 输出约 977 tokens，继续沿用 12k input/4k completion 与 0.13 USD 上限，不通过提高预算掩盖合同冗余。相关离线/文档测试 39/39 通过；任何 V4 外呼仍必须完成最终全量回归、文档后固定 parity 和新的 exact SHA 授权。

Compact V4 exact request `f96a2ab6...af1b1` 已获授权并完成：六题均首轮成功，6 provider calls、0 controlled retry、42,476 prompt tokens、16,285 completion tokens、0.06 USD reserved ceiling。六题 rubric 全部 pass，pair pattern 完全一致：每题 17 个 `distinct`、4 个 `shared_evidence_distinct_behavior`、0 个 risk findings；无 reviewer inconsistency。与原六份 candidate pass 合并后 cohort=`screening_ready`、`screening_eligible=true`。结果 SHA 为 `d2a9dee6...0f840`，manifest SHA 为 `bb66391e...b2202`。该结论仅允许制定受限 12 题 matched screening 的新申请；专业有效性仍为 provisional，本次未授权或执行 screening、solver、grader、training、release 或 promotion。

## 八、文档治理规则

阅读顺序：

1. 本文件：项目目标、当前状态和路线；
2. `AGENTS.md`：工程执行和安全边界；
3. `docs/README.md`：活跃文档索引与生命周期；
4. `docs/architecture/system_architecture.md`：当前技术结构；
5. 具体接口或 production contract；
6. 只有需要追溯决策时才阅读 `docs/archive/`。

更新规则：

- 每个阶段关闭时，只把核心结论同步到本文件。
- 详细计划和执行报告只管理局部问题，不得重复维护全局状态。
- 临时 workstream 计划必须由本文件链接；完成后迁入 archive。
- archive 原则上只读，不因当前状态变化而回写历史结论。
- 生成型 runner 默认只能写 artifacts，不能写活跃 docs。

## 十、两路线 Matched Screening V2

截至 2026-07-21，replicate B 的六个新包及独立 Reality gate 已全部通过，Compact V4 六题一致性复核也全部通过，12 个 assignment 均具备 reality-pass 证据。当前尚未进入业务 screening，正在关闭官方 DeepSeek 公共 solver preflight 的执行治理缺口。

用户授权的 preflight request `789f4f85...e742a3` 未被消费：在 receipt 生成前发现当前 matched-screening 模块只有 request compiler，缺少 receipt、执行状态与证据审计入口。该 request 现按 `superseded_pre_execution_no_receipt` 保存，外部调用、私有数据上传和 grader 调用均为零。项目已增加专用的 receipt/runner/audit 薄层，复用既有 Stirrup wrapper 和预算/交付合同；完成文档同步、全量测试及新 parity 后必须重新生成 SHA 并等待授权。

replacement request `33fa2c1d...bc061f` 获授权并生成 receipt 后，在 provider/solver 子进程前被 fixture 结构校验阻断：冻结目录只有 case 文件，没有 rw-task 所需的外层 dataset root 与 `dataset_row.json`。外部调用仍为零，且没有私有数据上传。当前修复把 public fixture 编译纳入正式 runner，并要求 request compiler 在生成 SHA 前验证完整 dataset-row 合同；该 receipt 不得复用，修复后必须再次完成本地/parity/exact authorization。

最终 DeepSeek public preflight request `c6e149b5...3bf0f1` 已消费并通过：单 session、6 provider calls、零 retry，create/copy/edit/save/submit 和精确可打开 XLSX 交付全部成立，合同成本约 0.25901 USD。solver selection 已审计 report、outcome 与 process-log hashes 后冻结为官方 `deepseek-v4-pro`；不再修复或切换 solver preflight。

12 题业务 screening 的离线执行闭环现已实现：exact business receipt、每题单独 solver session、delivery gate、route-blind DeepSeek grader、七维程序重算、离线 Validity/Utility/skill-causal 复核与 cohort 聚合均进入同一 fail-closed runner。当前尚未执行任何私有 12 题 solver/grader call；必须完成文档同步、全量本地测试、固定 parity 并生成新的 business exact SHA 后再次等待授权。

首份 business request `d9373c88...cb1b9b` 在 provider 前冻结为 `incomplete`：12 个 solver 进程均因 runner 把 case 目录而非其外层 dataset root 交给 rw-task 而未发现测试用例，solver/grader calls 均为零。该 receipt 不得复用。业务 runner 改为在 request-SHA 输出下为每题创建 route-blind 单 case dataset view，复制后重算并匹配冻结 candidate-tree hash，再把外层目录交给未修改的 Stirrup wrapper；新增回归必须直接拒绝 case-root 形态。

Replacement business request `f3addd1f...337873` 已根据项目级持续外部调用授权消费并完成。12/12 solver sessions 均产生实质 task failure，0/12 有效交付，零 infrastructure failure、零 grader calls；共 56 provider calls、5.22242 USD contract cost。失败分为 7 次 tool-call arguments JSON 截断和 5 次单题 contract-cost reservation 阻断。两条路线各自 offline validity、productive complexity、skill causal 均为 6/6，但 exact delivery 和 professional plausibility 均为 0/6，因此最终 decision=`redesign_required`，result SHA=`5479b333...a69de0`。不启动 confirmation、训练、promotion 或 release。

阶段执行更新（2026-07-21）：首份 generation request `9794d685...18a4` 已消费，7 次 `gpt-5.6-sol` 调用、116,671 provider tokens、14 USD 合同预留，得到 4/6 materialized。cross-check skill-guided 的已持久化 proposal 因 `corroborating` 与 `support` 未被内容门对齐而阻断；保守同义映射修复后已零 provider 回放通过，原始 block 保留。policy skill-guided 因 provider 字段 `display Name` 导致无可持久化 draft 的 schema normalization failure，已从误导性的 pending 冻结为 blocked。该失败不允许复用旧 receipt；下一外部边界仅可是绑定该失败、单 assignment、单 call、2 USD 的 replacement-generation exact request。Reality、solver 与 grader 均未启动。

Reality Cohort 已达到 `screening_ready`。下一阶段不再执行旧 V31 的三路线四 motif 清单，而固定为 `skill_guided_llm` 与 `llm_led_hybrid` 两条重构路线、三个非实验 motif、每个 motif 两个 matched briefs，共 12 个 route-blind packages。现有六包作为 replicate A 保持不变；replicate B 使用相同正式 public-source substrate、skills 和 capabilities，但必须形成新的 brief、subgraph 和 governed scenario 身份。`strict_template` 与 experimental `evidence_to_deliverable` 被排除。

本地 V2 合同、生成申请、盲化与 Reality 衔接、DeepSeek solver adapter、DeepSeek→Gemini 确定性选择、单次 DeepSeek grader 和 screening 聚合已经进入实现。当前仍未发生 replicate B provider、solver 或 grader 调用；第一外部边界只是新增六包的 immutable generation exact request。后续 Reality、公共 preflight 和业务 screening 仍需各自的新 SHA 授权。任何 screening 结果只决定 confirmation 资格，professional validity 保持 `provisional`，不授权训练、release、registry 或 promotion。
## 2026-07-21：E2B Codex 最小兼容性切片

冻结 DeepSeek–Stirrup 的 12 题 `0/12` 结果，不再把适配层修复作为当前主线。新增一条严格旁路的 Tuzi `gpt-5.6-sol` × E2B 官方 `codex` template 切片：先用公共 XLSX 探针验证 Tuzi Responses API、Codex JSONL、命令执行和真实 XLSX 写入；只有公共探针通过，才上传同一 fan-in brief 的两个 route-blind candidate 包。该切片不使用 Stirrup、自建 provider adapter 或预算 ledger，不调用 grader，也不扩展到其余十题。

实现、完整本地回归和固定 Linux/amd64 parity 均已通过 344/344；parity 指纹为 `a8fb919ca2914ab10eabac0389c8c6cb04b9830d260852a2d286cb958b70aae3`，断网、只读根目录、无 provider 凭据且清理成功。一次性 scope `4fa7726c...6fd26` 随后被消费，但公共探针在创建 sandbox 前因执行进程没有 `E2B_API_KEY` 而冻结为 `incomplete`。结果中私有任务为 0、私有文件上传为 0、Codex/Tuzi 调用为 0、grader 调用为 0；因此这不是 Tuzi Responses 兼容性结论。receipt 不得复用，两道私有题未执行。若继续，只能先离线补齐 E2B SDK 身份注入契约，再建立新的单次 scope；不得把这次失败静默重跑。

E2B 显式身份注入 V2 随后通过本地与固定容器 345/345，parity 指纹 `c8995863946f849b2fd81f4a85fed53453c068e244ae7704c40e9213678412a8`。新 scope `fc0e898f...84b72` 成功创建官方 `codex` sandbox，证明现有 E2B key 可用；但 sandbox 内第一条预备命令 `python3 -m pip install --disable-pip-version-check openpyxl` 以 code 1 退出，执行器在 Tuzi key 注入和 `codex exec` 前冻结为 `incomplete`。本轮仍为 0 私有上传、0 Codex/Tuzi calls、0 grader calls，两道真实任务未执行。该 receipt 不得复用；当前未知量已经从 E2B 身份收缩为 stock `codex` template 的 Python/openpyxl 运行时准备方式，尚未形成 Tuzi Responses 结论。

随后公共 runtime 诊断确认 stock `codex` template 具有 Python 3.11/pip，且同一 `openpyxl` 安装命令可成功。runner 增加“单次安装、exit marker、保留失败输出”而未增加 retry；本地与固定容器 346/346 通过，parity 指纹 `1b91a6beef023800cdc3fb58764a9c7c27d5b7a70055bad5cf6d7fd0cb7831fc`。scope `9d3bcb57...f7e2a` 的公共探针通过：Tuzi Responses、Codex JSONL、Shell 与 XLSX 写入均已实证兼容。第一道真实任务 `ms_2c770ac494eb53b8` 也通过，交付 SHA `e4d1437e...fa24d1`、27,324 bytes、可打开、有非空 sheet 且不等同任何输入 workbook。第二题 `ms_7e4e35a9be2befda` 的独立 sandbox 在无 JSONL/无交付状态下 command exit 1，被保守分类为 infrastructure failure；切片最终 `incomplete`。本轮不重跑第二题、不调用 grader、不执行其余十题、不判断路线优劣；receipt 已冻结。
## 2026-07-27：Codex/E2B Screening 恢复结论

第二题在 scope `2aad9b1c...0e799` 下恢复成功，二题兼容性达到 2/2 pass。十题 solver scope `b3c7e7e0...7371d` 随后复用两份 pass 并执行十个新任务；冻结结果表面为 3/12 valid delivery，但审计发现九个非 pass 中八个是 Tuzi Responses stream disconnect，另一个是 runner 把所有任务交付路径误写为 `validation_workpaper.xlsx`，而真实包合同分别要求 `validation_workpaper.xlsx`、`review_memo.xlsx` 或 `task_deliverable.xlsx`。该结果不是可信路线交付率。

逐包交付路径与 stdout transport 分类修复后，本地/固定容器 347/347 通过，parity 指纹 `f11348dd...9e2e5`。九题 recovery scope `1f6d2e7b...df4cf` 随后执行：前五题全部出现 Responses stream/HTTP incomplete-body 基础设施失败；第六题超过冻结的 1,800 秒但 E2B SDK 未返回 timeout，本地 orchestration 被终止，后三题未运行。当前保留三份有效交付，grader 为 0 calls，campaign=`incomplete`，不形成路线比较或 confirmation 资格。receipt 不得复用；不再通过重复重跑掩盖系统性流故障。

## 2026-07-30：本地 Codex CLI 同质化 Screening

当前主线已停止修补 E2B、Stirrup 与 Tuzi solver 链。旧 E2B 三份有效交付只保留为基础设施诊断，不进入路线比较。新的唯一活跃 solver 路径是项目忽略目录中的官方独立 `@openai/codex` CLI，复用本机现有 ChatGPT 登录并固定 `gpt-5.6-sol`；十二个盲包全部从头执行，不混入任何旧交付。

本地执行器只把 `dataset_row.json`、`deliverable_contract.json` 和 `reference_files/*.xlsx` 投影到每题独立工作区，并固定 `codex exec --json --ephemeral --ignore-user-config --ignore-rules --sandbox danger-full-access --skip-git-repo-check`。这里的 `danger-full-access` 只关闭会错误继承桌面托管只读策略的第二层 Codex 沙盒；执行器本身仍由外层托管工作区边界隔离，且不会使用 `--dangerously-bypass-approvals-and-sandbox`。每题一个进程、一次尝试、1,800 秒硬超时；启动前原子写入状态，中断任务不得静默重跑。认证文件内容不被读取或复制，模型 API key 环境变量从子进程环境中剔除。只有精确路径、非空、可由 `openpyxl` 打开、有实际内容且不等同任一输入 workbook 的 XLSX 才能进入保留的官方 DeepSeek 七维 grader。

实现与定向回归已经开始落盘；在最终文档同步、完整本地回归和断网/只读/无凭据 Linux parity 完成前，尚未安装冻结独立 CLI、尚未运行公共探针，也没有发生新的十二题 solver 或 grader 调用。parity 只证明治理代码可复现，不宣称容器复现本机 Codex/ChatGPT 行为。公共探针通过后将按同一 scope 顺序执行全部十二题；最终结果仍只可能是 `confirmation_ready_both`、`single_route_confirmation_candidate`、`redesign_required` 或 `incomplete`，professional validity 保持 `provisional`，不授权 confirmation、训练、promotion 或 release。

首次本地 scope `af28364d...bdf48` 已消费并冻结 `incomplete`：官方 CLI 与 ChatGPT 登录、`gpt-5.6-sol`、正常 turn/command JSONL 均已进入，但桌面宿主注入的 `CODEX_PERMISSION_PROFILE` 覆盖了显式 `workspace-write`，导致 Python 和写文件命令被内部 policy 拒绝。公共探针无交付，十二个私有任务保持 0/12，grader 为 0 calls。这不是模型能力失败，也不允许复用 receipt。修复仅删除子进程继承的内部 `CODEX_*` 宿主标记并重新设置 `CODEX_HOME`，同时关闭 plugins/apps/multi-agent/skill-search 与 project-doc 注入。

第二个本地 scope `bab6bd1...fdd` 因尝试覆盖保留的内建 `openai` provider retry 字段而在模型调用前失败，0 私有任务、0 grader；该字段只对 custom provider 开放，现已从活动命令删除。第三个 scope `db1dd24...9e4` 证明清除环境变量仍不足以解除桌面托管的只读策略：公共探针正常完成模型 turn 和多次 command 规划，但全部 Python/写入命令被内部 policy 拒绝，仍为 0/12。活动合同因此显式采用“外层托管工作区隔离 + 内层 Codex `danger-full-access`”，而不是继续修补 provider 或沙盒适配器。

本地 Codex scope `a23babbc...a3fa0` 已完成真实业务执行。公共 XLSX 探针通过后，`gpt-5.6-sol` 使用十二个独立本地 `codex exec` 进程完成 12/12 精确、非空、可打开、非输入复制的 XLSX 交付；两条路线均为 6/6，0 solver retry、0 infrastructure failure，总执行时间约 42.7 分钟。交付物为 5–11 个工作表、约 19.9–34.0 KB。该证据推翻旧 DeepSeek–Stirrup `0/12` 对任务可执行性的表面印象：旧失败属于 solver 链，不是这十二题无法被强模型完成。

保留的官方 DeepSeek grader 随后完成 12 个评分阶段，共 19 calls、204,768 prompt tokens、72,726 completion tokens 和约 1,014 provider seconds；只有 6 个形成合法七维记录，另 6 个在唯一格式补跑后仍失败。19 次响应中 12 次在 4,000 completion-token 上以 `finish_reason=length` 结束，因此最终 decision 按合同为 `incomplete`，而不是路线失败。两条路线各有 3 个合法评分，且六个合法记录全部为 1.0，继续暴露明显 score saturation。当前已解决的是 solver 主线；未完工点收敛为 grader 输出合同压缩/校准与评分饱和，不应再回到 E2B、Stirrup、Tuzi solver 或任务生成重构。professional validity 仍为 `provisional`，confirmation、训练、promotion 和 release 均未启动。

## 十二、本地 Codex 验证后的宏观路线修订

本项目现在允许把本地 Codex 提升为**项目内部默认行为执行器**：它负责候选侧盲包的真实文件工作、交付检查和后续 teacher-solution 候选生产。该提升只替换 active solver backend，不代表 `gpt-5.6-sol` 在所有环境均已验证，也不代表任一任务生成路线、rubric、训练数据或 production release 已获推广。E2B、Stirrup、Tuzi solver adapter 和 DeepSeek solver 路径退出 active workstream，保留为只读历史诊断，不再投入兼容性开发。

后续 F 阶段按五个结果导向步骤推进：

1. **F5 行为执行后端收口——完成。** 固定本地 Codex CLI、route-blind 投影、一次性进程、精确 XLSX 交付门和可恢复状态。以后新 screening 默认复用该薄执行链，不再为每个 provider 建 agent adapter。
2. **F6 评价器重建——下一阶段。** 复用十二份冻结交付和十九份 DeepSeek 原始响应，离线设计小于约 1,000 completion tokens 的紧凑 grader 合同；维度分、major defect 和少量 evidence locator 分离，先用固定响应证明不会截断，再从头同质重评 12 份交付。准入要求是完整评分覆盖、无系统性截断、分数不全饱和，并抽取至少四题进行独立专业复核。缺失评分不得补成零分。
3. **F7 代表性规模试产。** 在不增加新路线和实验 motif 的前提下，将两路线、三种正式 motif 扩到约 24–30 个新任务，并至少覆盖现有财务场景之外的一个已验证领域。任务生成、Reality gate、本地 Codex 执行、紧凑 grader 和抽样专业复核组成一条流水线。这里衡量交付率、major defect、评价稳定性、任务重复度和操作成本，不再以 schema/test 数量当主要进度。
4. **F8 路线收敛与数据准入。** 只有当两条路线都有完整且非饱和的绝对质量证据时才比较路线。如果质量近似，优先选择实现更简单、调用更少、维护成本更低的路线作为默认生产候选，另一条保留为 challenger；不为了得到“胜者”继续扩大复杂治理。通过任务进入候选训练池前，必须同时具备 source provenance、candidate/teacher 隔离、真实交付、grader evidence 和抽样专业有效性。
5. **F9 小规模训练验证。** 先形成可审计的 SFT 小样本，使用任务、候选可见材料、最终交付和结构化反馈；不得把隐藏推理、原始 Codex 会话或认证信息作为训练数据。只有 SFT 相对未训练基线出现可复现收益、评价器可靠且无重大有效性问题后，才单独讨论 reward model 或 RL。

宏观停止规则也相应简化：F6 未解决 grader 截断与饱和，不扩量；F7 暴露系统性任务缺陷，返回 task-design/rubric 的具体问题层，而不是重写执行框架；F8 无显著路线差异时按简单性收敛，不继续制造比较层；没有专业有效性和训练收益证据时，始终停在候选训练池。

F6 Compact Grader V2 的离线实现现已落地，且不改写原 12 题 solver 或 V1 grader 证据。新合同使用固定七项 0–4 分：3 表示完整、专业可用的正常通过，4 只允许“超出明确交付合同”的卓越表现并必须绑定具体 exceptional evidence；2 分及以下必须绑定扣分 finding。输出不再为每个 criterion 重复长 rationale/locator，只允许最多五条问题和两条卓越证据，weighted score、professional plausibility 和有效维度由程序计算。真实十二题完整消息估算为约 10.3k–17.4k input tokens，标准输出小于 1,000 字符。首次执行采用的 20k input/2k completion 是离线估计，已被下面的真实 high-thinking 截断证据推翻；Compact V2.1 因此保留相同评分合同，只把 completion 上限纠正为 4k。

F6 的首次外部 Compact V2 scope `ac8fcb1d...ff427b` 已及时暂停并冻结为 `incomplete`：最终为 2 题完成、2 题 infrastructure failed、1 题 interrupted、7 题未启动。两份成功评分均为七维 3 分、weighted score 0.75、无 major defect，证明紧凑输出和“正常通过不等于满分”的校准方向有效。两道失败题的四次尝试则都在恰好 2,000 completion tokens 时以 `finish_reason=length`、空可见内容结束；这是 DeepSeek high-thinking 与过小 completion ceiling 的接口预算不匹配，不是任务、交付物或 rubric 失败。该 scope 不得续跑，也不得把两份成功结果混入最终 cohort。推荐下一步为 Compact V2.1：保持冻结输入、紧凑 schema、七维尺度和 high-thinking，只把 completion ceiling 恢复为 4,000；通过新回归、parity 和 grader-only scope 后，对 12 份交付从头同质重评。若 4k 仍系统截断，再暂停评估是否关闭 thinking。本阶段不重跑 Codex、不造新题、不进入训练。

Compact V2.1 已按上述边界完成。完整本地与固定 Linux/amd64 parity 均为 372/372；最终指纹 `f3d52e54...60271`，容器断网、根目录只读、无 provider credentials 且清理成功。grader-only scope `bc38a3d7...3d6f6` 对十二份冻结交付从头同质评分，12/12 完成、0 infrastructure failure、16 calls，其中 4 次为合法格式补跑。十一题 weighted score 0.75，一题 0.70；12/12 professional plausibility pass、0 major defect、0 route identity seen。两条路线均为 offline validity 6/6、有效交付 6/6、productive complexity 6/6、skill causal 6/6，六个 matched pairs 全部可比较，程序结论为 `confirmation_ready_both`。这关闭了当前 screening 的 solver/grader 缺口，但专业有效性仍为 `provisional`：分数高度集中在“正常合格”的 0.75，尚不足以证明 evaluator 对细微质量差异敏感，也不构成路线优胜、专家证据或训练准入。下一步是跨模型、route-blind 的六题专业校准，然后才决定是否进入 24–30 题代表性生产试点。

六题跨模型专业校准已完成离线实现：从 2 routes × 3 production motifs 的每个 cell 固定抽取一题，并让样本同时覆盖 replicate A/B；独立 reviewer 固定为 Tuzi `gemini-3.1-pro-preview`。Gemini payload 不包含 DeepSeek/Compact 分数或路线身份，只包含候选要求、冻结 rubric/fact anchors 和实际交付 XLSX；模型响应落盘后，程序才比较 42 个 criterion scores。预先固定的一致性门槛为 professional plausibility 至少 5/6 一致、major defect 6/6 一致、平均绝对维度差 ≤0.5、最大单维差 ≤1。每题一次实质判断，仅格式/传输失败可补跑一次，最多 12 calls；结果仍只是 `llm_proxy`。定向测试 15/15 通过，须完成全量回归与新 parity 后才能编译执行 scope。

首次编译的 calibration scope `74ec9ac1...d9871` 在 provider entry 前停止，外部调用和上传均为 0：本地 ignored `.env` 使用既有 `AGENT_API_KEY/AGENT_BASE_URL` 命名，而新 runner 只读取了 `OPENAI_*`。当前 loader 已复用项目既有 AGENT/GRADER aliases，仍不改写或输出 `.env`；执行入口同时新增源码指纹、Compact scope/outcome 和 parity hash 的运行时复核。因此该旧 scope 随代码指纹变化作废，不得执行，必须重新完成回归、parity 和 scope 编译。修复后的相关测试为 21/21。

配置修复后的 replacement scope `4ce10cc6...0baf8` 也在 provider entry 前 fail closed，调用和上传仍为 0：compiler 对 LF 文本计算 SHA，但 Windows `write_text` 落盘为 CRLF 字节，导致 scope 文件名与实际文件 SHA 不同。该模块现已复用 Compact grader 的成熟 canonical bytes 写法：排序 JSON、固定 LF 尾部、UTF-8 `write_bytes`、碰撞检查，并增加文件名字节哈希回归。旧 scope 不得改名或绕过校验，必须再次以新指纹编译。

最终 canonical scope `920977c2...f01f65` 在完整本地/固定 parity 378/378、指纹 `e053d45a...d2284` 下通过文件名与实际字节 SHA 独立核对并进入 Tuzi Gemini。执行在观察到重复合同失败后被主动停止并冻结为 `incomplete`：0 completed、2 infrastructure failed、1 interrupted、3 not started，共 4 provider calls。四份响应均为 `finish_reason=stop` 且内容完整，不是 token 或传输问题；Gemini 在两题的四次响应中都把七维全部打 4，却只提供两条 exceptional evidence，违反“每个 4 必须绑定证据”的冻结尺度。其中一次另有多余 `}`，补跑又缺少 `findings`。这表明该 reviewer 对当前校准合同系统性不遵循，并重新表现出满分饱和；不能把 raw 4 分当作有效专业结论，也不能据此修改任务。R8.2 暂停，R8.3 生产扩展不得启动；下一步需先选择真正独立且能遵守合同的 reviewer，或把专业校准交给人工域专家。本阶段仍不训练。

## 十三、当前收口点与后续宏观计划（2026-07-31）

项目已经完成“任务能否被强模型真实执行”的关键验证：固定本地 Codex CLI 与 `gpt-5.6-sol` 对十二个 route-blind 包实现 12/12 有效 XLSX 交付，两条路线各 6/6；Compact Grader V2.1 又完成 12/12 同质评分，程序结论为 `confirmation_ready_both`。因此，本地 Codex 现作为项目内部默认行为执行后端继续推广，E2B、Stirrup、Tuzi solver adapter 与 DeepSeek solver 不再是活跃优化方向。这里的“推广”只指复用稳定执行链，不等于生成路线、rubric、训练数据或 production release 已获准推广。

独立专业有效性校准仍是证据缺口。Gemini 校准因系统性违反评分合同而冻结，不能解释为任务失败。项目已生成六题人工域专家盲审包作为未来可选补证，但用户在 2026-07-31 明确表示当前没有人工专家，并同意以“现有 AI proxy 对受限工程试点足够合格”为工作假设跳过此门槛。因此状态改为 `human_expert_review_deferred_by_user`；不得把这一选择改写为已经取得人类专家证据。

后续主线收敛为四步：

1. **R8.2 AI-proxy 试点豁免——完成**：Reality、12/12 本地 Codex 有效交付和 Compact V2.1 共同作为试点级证据；professional validity 继续标记 `provisional_ai_assumed_sufficient_for_pilot`，人工包保留为未来可选补证。
2. **R8.3 代表性生产试点——当前主线**：生产 24 个新任务，固定为 2 routes × 3 production motifs × 2 domains × 2 fresh briefs。两个领域分别为已有 audit/compliance 与新增、需先完成正式来源/skill 验证的 procurement/operations；统一使用本地 Codex 行为执行链，测量真实交付率、缺陷、重复、审查稳定性和操作成本。
3. **R8.4 路线收敛**：先看绝对质量门槛，再做 matched comparison；若两路线实质相当，选择更简单、成本更低的一条作为 production candidate，而不是继续制造新的比较框架。
4. **R8.5 训练数据准入设计**：只整理 source-bound、Reality-screened、真实执行、合格评分且经过专业抽样的候选记录；本阶段只形成可审计数据集与准入报告，不启动 SFT、RL 或任何训练。

生产试点的准入不再等待人工专家，也不再更换或适配 solver。该豁免只允许 report-first 的 24 题试点：不得将其解释为专家证据，不自动确认任何路线，不进入 production release，也不启动训练。后续工程工作只能直接服务于新来源/skill 验证、任务生成、Reality、真实执行、评分和试点聚合。

R8.3 已开始执行新增 `procurement_operations` 来源门。Serper topic 搜索首次返回 0 个可采集结果；新增的现有收集器精确 URL 模式随后发现本机透明代理把公开域名映射至 `198.18.0.0/15`。安全校验现只对“HTTPS 域名 + TLS 主机名验证 + 该透明代理保留网段”开放，HTTP、裸 IP、localhost 和其他非公网地址继续阻断。三条 Acquisition.gov 官方来源（FAR Part 13、Part 46、32.905）已 3/3 采集、3/3 source-quality pass、0 warning，registry 未修改。

首轮官方 `deepseek-v4-pro` source-to-skill 抽取一次完成：6 个候选、5 accept、1 revise、0 reject，graph extraction 为 ready，18 resources、3 trace edges；但自动 motif hints 只给出一个可用 fan-in 和一个被禁用的 E2D，产生 `motif_coverage_below_trace_coverage`。第二组更窄的三条官方条款同样 3/3 source-quality pass，但唯一抽取响应在 12k completion 上限处以 `finish_reason=length` 截断，未形成候选且不重跑。首轮接受的 invoice validation、threshold/competition analysis、quality inspection/nonconformance skills 随后被显式映射到 cross-check、policy 与 fan-in briefs；加上全新身份的 audit/compliance briefs，正式 admission 达到 12/12 pass，并编译出 24 个唯一 route assignments。

上述精确 URL 与安全边界变更已通过完整本地 380/380 和固定 Linux/amd64 380/380；parity 指纹 `8e867b2a...c4877`，网络关闭、根目录只读、无 provider credentials 且 cleanup 成功。该 parity 只授权后续编译本地 pilot 输入，不授权外部生成、registry mutation、release 或训练。

R8.3 brief/generation 实现的最终回归与固定 parity 已提升为 385/385，source fingerprint `82ae7770...df90`；容器仍为断网、只读根、无凭据且 cleanup 成功。生成阶段随后执行完全部 24 个 assignments：24 次正常首轮加 2 次合法 feedback repair，共 26 次 Tuzi `gpt-5.6-sol` 调用；两次 repair 均成功，0 provider/infrastructure failure，累计 333,544 tokens 和约 3,596 provider seconds。

冻结的 provider-generation 结果为 22/24 materialized，22 个 package fingerprints 全部唯一；skill-guided 为 12/12，LLM-led 为 10/12。两个 LLM-led block 分别位于 audit/compliance × cross-check replicate A 与 procurement/operations × fan-in replicate B，均触发 `candidate_output_modeled_as_reference_input`。逐字段审查随后确认这是同一确定性门禁假阳性：两个 reference 分别“用于支持候选人后续映射”和“要求候选人后续进行严重度分类”，并非把最终交付物预先提供给候选人。

全局 content validator 现只在明确的 input-to-output 关系语境下排除该短语，真实 candidate-output leak 的负例仍会阻断。两个原 proposal 已零 provider 调用完整回放，proposal、content、provenance、render、export 和 candidate/teacher isolation 全部通过。新的 package-readiness 记录不改写 22/24 历史结果，而是绑定 22 个原包和 2 个确定性假阳性回放包；当前为 24/24 `packages_ready`。

因此 R8.3 当前准确状态是“12 briefs 已准入、24 assignments 已执行、24 packages 已通过确定性准入，正在进入独立 Reality proxy”。JSON-safe payload 修复后的完整本地套件为 392/392。新 Reality 合同固定先审查 24 个 route-blind candidate packages，再审查 24 份 Compact V4 rubric；绑定包、候选树、rubric、确定性 audit、源码与 parity 哈希，官方 `deepseek-v4-pro` 的 SDK retry 为零，每阶段仅允许一次格式/传输补跑。只有最终固定 Linux/amd64 parity 通过后才编译 scope，且只有完整 `screening_ready` 才进入本地 Codex；Compact grader 和训练仍未启动。

首个 24 包 Reality scope 已在 provider 前冻结：持久化首题 input 时，XLSX 提取结果中的 `datetime` 不能直接 JSON 序列化。manifest 为 `incomplete`，provider calls=0、completed stages=0、没有外部上传；该 scope 不复用。执行器现先生成唯一 JSON-safe payload，再以同一对象写证据并提交 provider；修复后必须重新跑完整本地套件、文档后 parity，并创建新的 scope。

replacement Reality scope 已完成。修复后的本地与固定 Linux/amd64 套件均为 392/392，source fingerprint=`e5db87bb...9dcf4d`；官方 `deepseek-v4-pro` 按顺序完成 24 个 candidate-blind 和 24 个 Compact V4 rubric stages。共 51 calls，3 次首轮 length 截断均使用唯一受控补跑并成功；总 usage 为 320,167 prompt、142,210 completion tokens。120/120 candidate dimensions pass，24/24 rubric pass，每题均为 17 distinct + 4 shared-evidence-distinct-behavior、0 risk findings，0 reviewer inconsistency。最终 `screening_ready`，结果 SHA=`db897fed...b631`。当前进入本地 Codex 24 题执行；grader、路线收敛和训练尚未启动。

本地 Codex 扩展保持最薄：复用已验证的同一进程执行、公共探针、route-blind 投影、30 分钟超时、零 runner retry 和 XLSX 交付检查，只把 scope 从历史 12 题矩阵扩展为含 domain 的 24 题矩阵。固定 CLI 仍为 `@openai/codex` 0.146.0，使用现有 ChatGPT 登录与 `gpt-5.6-sol`；未重新引入 E2B、Stirrup、Tuzi solver adapter 或预算门禁。定向新旧兼容测试 16/16、同步后的完整本地回归 395/395 已通过，下一门是最终固定 parity。

首个 V2 scope 在 receipt 前因树哈希接口误配而 fail closed：Reality/readiness 使用 `_tree_sha`，旧 Codex 执行绑定使用 `_tree`，编译器错误地跨算法比较。核对显示 24/24 package 本体、24/24 candidate tree 和 Reality pass 均未漂移。修复仅让上游验证沿用 `_tree_sha`、Codex 执行绑定继续使用自身 `_tree`；没有任务包变更、没有 Codex 调用，也没有 receipt 消费。修复后的完整本地套件为 396/396。

修复后的固定 Linux/amd64 parity 同样为 396/396，source fingerprint=`5f5a5731...c3e5df`。content-addressed scope `3c215519...4852a7` 随后完成本地 Codex 执行：公共探针 pass，24/24 独立任务均生成 exact-path、非空、可打开且非输入复制的 XLSX，零 task failure、零 infrastructure failure、零 runner retry。两条路线各 12/12、两个领域各 12/12、三个 motif 各 8/8；任务进程总耗时约 4,556.6 秒，记录 4,763,981 input（其中 3,960,064 cached）与 186,809 output tokens。solver manifest SHA=`4cf2c7dd...214d4`。当前进入 Compact grader，训练仍未启动。

第二个 scope `bab6bd17...6dfdd` 在 provider 前 0.5 秒冻结：官方 CLI 拒绝为保留的内置 `openai` provider 覆盖 `request_max_retries/stream_max_retries`，该配置只对自定义 provider 开放。仍为 0 私有任务和 0 模型回合。项目不为此建立 custom provider 或转换器；新合同准确固定为“每题一个 Codex 进程、runner retry=0”，并明确记录 ChatGPT 内置 transport reconnect 不可由用户配置。修复后必须重新完成完整本地/parity、编译新 scope，再从公共探针开始。

R8.3.7 采用现有 Compact Grader V2.1 的同一评分提示词、七维 0–4 尺度、4k completion 上限和一次格式/传输补跑，只把 cohort 合同向后兼容地扩展为 24 题。代表性门槛按原 6 题合同同比例扩展：每路线 12 题，offline validity 与 skill causal 必须 12/12，有效交付、professional plausibility 和 productive complexity 至少 10/12，major defect 必须为 0，饱和分不超过 8/12；同时报告两个领域各 12 题和 12 个跨路线 matched pairs。该结果只形成 report-only production-candidate 建议，不授权训练、发布或 promotion。

该代表性 grader 扩展已通过完整本地与固定 Linux/amd64 398/398，source fingerprint=`aded61fc...e959`。scope `5cfeed30...dffe` 随后对 24 份冻结交付物完成一次同质运行，未重跑 Codex；最终为 22 份有效评分、2 份 infrastructure failure、30 次 provider calls，cohort 按合同冻结为 `incomplete`。22 份有效评分全部 professional plausibility pass、0 major defect、0 route identity exposure；20 份为 0.75，另两份为 0.70 和 0.6625，0 个饱和分。两个失败恰好来自 procurement cross-check replicate B 的两条路线：一题两次均在 4,000 completion tokens 截断且无可见内容；另一题首轮给出实质可用但字段超长的 JSON，唯一补跑又在 4,000 tokens 截断。现有 11 个 matched pairs 完整。当前卡点再次明确收敛到 DeepSeek grader 的输出合同完整性，不是任务生成、Reality、Codex 执行或 XLSX 交付失败；不得补零分、截断字段后追认或复用旧 scope。R8.3.8 暂停，训练仍未启动。

下一次恢复不再提高 token 上限或更换任务链，而采用早已预留的 non-thinking fallback：Compact V3.1 保留同一 24 份冻结输入、同一七维 rubric、同一 JSON schema、4k completion 和一次格式/传输补跑，仅关闭 DeepSeek 会占用 completion 配额的 thinking。新 cohort 必须 24 题从头同质评分，不混用上轮 22 份结果；若 24/24 完成则直接进入程序聚合与 R8.3 收口，若同类完整性问题再次出现则暂停路线收敛，不继续制造 grader adapter。任务、Codex solver、rubric 和训练均不在本次变更范围。

V3.1 已完成验证与实跑：完整本地/固定 Linux 回归均为 399/399，source fingerprint=`bba2d794...ef27`；scope `c4b4c855...61ad` 从头评分 24 题，最终为 21 份有效评分、3 份 schema-contract failure、32 calls，仍为 `incomplete`。关闭 thinking 后六个失败响应都正常结束，completion 仅 261–598 tokens，证明截断已消失；新失败主要是 finding/exceptional-evidence 文本超过 180/240 字符，另有一次 4 分缺少对应 exceptional evidence。21 份有效评分全部 professional plausibility pass、0 major defect、0 route exposure，16 份 0.75，另外 5 份为 0.775–0.825，0 饱和分，但只有 9 个 matched pairs 完整。由此可确认问题不是任务、solver、交付物，也不再只是 thinking token 消耗，而是当前 DeepSeek 与过细 grader 输出约束的稳定兼容性。按预设停止规则，R8.3 在路线收敛前暂停：不混用两轮评分、不裁剪文本追认、不静默放宽 schema，也不继续制造适配器；训练仍未启动。

后续评价决策现已修订为停止 DeepSeek grader，直接复用已经 24/24 稳定的本地 Codex CLI 原生结构化输出能力。CLI 自带 `--output-schema` 与 `--output-last-message`，因此无需 provider adapter：每题新建 route-blind 评分工作区，只含候选要求、冻结 rubric、确定性 fact anchors 和实际交付 XLSX；一个独立 Codex 进程检查文件并写出 `grade.raw.json`，程序再校验七维并重算分数。每题一次进程、零模型重试。由于生成、solver 和 grader 都是 `gpt-5.6-sol`，证据必须标记为 `same_model_behavioral_proxy`，不能声称跨模型独立性或专家有效性；它只用于用户已批准的 report-only 工程试点和路线简化判断，不授权训练、release 或 promotion。

首个本地 grader scope `e41ac3b1...51bee` 在模型推理前被冻结。Codex/Responses 请求校验要求 strict JSON Schema 把 `properties` 中每个字段都列入 `required`，而原始 Pydantic schema 没有把带默认值的 `findings`、`exceptional_evidence` 等字段列入；前五题因此产生相同 400，随后立即终止，另一个 in-flight 记录按 interrupted 落盘，0 有效评分。修复只在提交前递归编译 strict schema：所有属性 required、`additionalProperties=false`、移除不被 strict subset 接受的 default，同时保留 nullable 类型和所有评分约束；不会修改模型响应或任务数据。旧 scope 不续跑，必须在新回归/parity 后编译新 scope。

replacement 本地 grader scope `37a227d9...e9cf` 已完整结束：24/24 有效结构化评分，0 infrastructure failure、0 grading failure、0 retry，程序结论为 `production_candidate_both`。两条路线均为 offline validity、有效交付、professional plausibility、productive complexity、skill causal 12/12，major defect=0、饱和分=0。grader manifest SHA=`e12588f...b1ffa`，outcome SHA=`57fd039f...6700`。这关闭了 R8.3 代表性试点的任务生成、Reality、真实执行和评分主链。

R8.4 的确定性路线收敛报告 SHA=`0eaa56f1...a2077`。两路线质量没有形成实质胜负：skill-guided 均分 0.811458，LLM-led 均分 0.795833；12 个 matched pairs 为 skill-guided 6 胜、LLM-led 5 胜、1 平。简单性差异则明显：LLM-led 的 12 个候选包共有 52 个 XLSX、329,373 bytes，skill-guided 为 101 个 XLSX、629,370 bytes。因此按预先声明的简单性 tie-break，`llm_led_hybrid` 被推荐为 **report-only production candidate**，`skill_guided_llm` 保留为 challenger/control。该记录不修改默认生成链、registry、release 或 promotion。

当前主线进入 R8.5：只设计并编译候选训练数据准入清单，核对 source provenance、候选/教师隔离、Reality、真实交付、评分、血缘和泄漏边界；本阶段不执行 SFT、RL、reward model 或其他训练。由于人工专家仍被用户明确延期，professional validity 保持 `provisional_ai_assumed_sufficient_for_pilot`，本地 grader 证据保持 `same_model_behavioral_proxy`。

R8.5 准入清单已经编译完成，SHA=`e229994a...6385`。24/24 记录满足 source-bound、候选/教师隔离、Reality-screened、真实有效交付、结构化评分有效和无 major defect；其中 12 条 LLM-led 标记为 production-candidate，12 条 skill-guided 标记为 challenger/control。它只是审计清单，不是训练集：允许未来另行审查的字段仅为候选 prompt、候选可见 reference、有效 solver deliverable 和结构化 grader feedback；teacher rubric、fact anchors、Codex JSONL、隐藏推理与认证材料明确禁止导出。当前 exported examples=0、dataset export=false、training authorized=false、training started=false。

R8.6 外部模型实用栈对比已经执行并冻结。执行前完整本地与固定 Linux/amd64 parity 均为 411/411，source fingerprint=`65a44da5...c6db0`；一次性 scope SHA=`1e4577de...16cc4`。冻结的 GPT 基线 `gpt-5.6-sol@chatgpt_codex` 未重跑，仍为 24/24 有效交付、24/24 完成评分、0 infrastructure failure、0 major defect，均分 0.803646。

两个外部栈均只使用本地 Codex 原生 Responses custom provider，没有回到 E2B、Stirrup 或自建 adapter。Gemini 公共 XLSX 探针在 7.328 秒后以服务高负载错误结束，没有命令事件、没有交付、没有创建任何私有任务工作区，24/24 私有任务保持 `not_started`。DeepSeek 公共探针在 64.094 秒内通过，随后真实任务连续暴露 Responses 流不稳定：共尝试 6/24，原始终止事件全部为 `stream disconnected before completion`，0 个正常完成、0 个 grader-eligible 交付，触发系统性冻结，剩余 18 题未运行。首版分类器曾因扫描完整 JSONL、误读任务正文中的 `unauthorized/403` 而把其中五次标成认证错误；该报告层误判已修复并增加回归测试，不改写冻结原始证据，也不改变六次均属 infrastructure failure 的结论。

最终 `ExternalModelComparisonResultV1` 为 `comparison_incomplete`。Gemini 和 DeepSeek 都没有达到至少 20/24 有效交付与 20 个共同评分任务的能力比较门槛，因此不能据此排名模型能力，也不存在实质胜者或 practical tie 结论。工程上能够确认的只有：当前 Tuzi × Codex Responses 实用栈不足以承担这批长时、多轮 XLSX 任务，而本地 ChatGPT 登录 × Codex 基线保持稳定。默认 solver、路线推荐、训练、release 与 promotion 均未改变。

### R8.7 官方 DeepSeek × OpenCode 同质评测

R8.7 不再把官方 DeepSeek 接到 Codex Responses，也不复用旧 DeepSeek–Stirrup adapter。新的实用栈固定为官方 `deepseek-v4-pro` 与 OpenCode：先用官方 API 完成 `/models` 和流式 tool-call 循环，再优先在 E2B 官方 `opencode` template 完成公开 XLSX agent 探针；只有 E2B/template 基础设施失败时才回退到固定版 WSL2 Ubuntu OpenCode。首个通过公开探针的环境必须独占整个 cohort，禁止混合 E2B 与 WSL 任务证据。

执行门槛固定为同一 audit/compliance × cross-check brief 的两道 matched 任务 `rp_003152533de5b652` 与 `rp_fb0d66050783b08e`。只有 2/2 产生精确路径、非空、可打开、有内容且不是输入副本的 XLSX，才自动继续其余 22 题。每题一个新环境/工作区、一个 `opencode run`、零项目级重试；OpenCode 原生 provider retry 只作为被观察的实用栈行为。连续三次相同认证、协议、服务或断流错误冻结 campaign。

有效 DeepSeek 交付只接受一次现有本地 `gpt-5.6-sol` 七维 grader 评分，因此对 DeepSeek 属于 independent-model proxy，但仍不是人工专家证据。至少 20/24 有效且全部完成评分后，才与冻结 GPT 24/24 基线做共同任务比较。结果只允许 `official_deepseek_comparison_ready`、`official_deepseek_partial` 或 `stack_incomplete`，不自动切换默认 solver、路线、训练、release 或 promotion。

当前实现已落盘：`OfficialDeepSeekOpenCodeCampaignV1`、官方 provider probe、E2B/WSL 确定性选择、OpenCode JSON 事件审计、XLSX 准入、matched 扩展门、一次性 grader 和配对聚合均已完成；定向测试 7/7、完整本地套件 419/419 通过。尚未完成最终文档后 Linux/amd64 parity，尚未编译/消费执行 scope，也尚未调用官方 DeepSeek、E2B 或上传私有任务。

最终 Linux/amd64 parity 随后以 419/419 通过，scope `5a8d5bb7...e1835` 被消费。官方 DeepSeek 公共 API 层完整通过：`/models` 包含 `deepseek-v4-pro`，流式 tool call、参数拼接、工具结果续接和最终文本均有效，共 737 tokens。执行在环境选择层冻结为 `stack_incomplete`，24 个私有任务全部保持 `not_started`。原因是两个本地公共环境检查缺陷：E2B 检测到缺少 openpyxl 后以 import 的非零状态抛出，未进入预定安装；WSL fallback 则错误传递了 bash 位置参数。两处均已离线修复并补充测试；这不是 DeepSeek/OpenCode 兼容性或任务能力证据。旧 receipt 不复用，必须重新完成全量回归、parity 和新 scope。

修复后的本地与 Linux/amd64 parity 均为 420/420，scope `56c134a0...ed8a6` 完成真实执行。官方 provider、E2B 官方 `opencode` template、OpenCode 1.17.13 和公开 XLSX 探针均通过；matched 两题 2/2 有效，因此其余任务开始执行。首七题全部产生有效 XLSX，随后发生一个 `RemoteProtocolError` 和两个连续 DNS `ConnectError/getaddrinfo failed`。原分类器只识别断流文本、漏掉 DNS 连接错误，因而未在第三次同类 infrastructure failure 后冻结，继续尝试了后续 14 题。

原始 execution/outcome 不改写。新增 `OfficialDeepSeekOpenCodeExecutionAuditV1` 仅从结构化诊断、stderr 和交付证据重算：合法前缀为 10 题，7 pass、3 `service_or_stream_failure`，冻结点为 `rp_44ce4deec29d8311`；后续 14 题全部标记 `out_of_scope_after_freeze`，不进入模型能力统计。七份有效交付各获得一次本地 grader 尝试，其中 3 份完成，4 份受本地 ChatGPT/Codex DNS/stream 基础设施故障影响；没有补跑。三份有效评分为 0.60、0.70、0.6375，其中 2/3 professional plausibility pass、0 major defect。

最终审计结果为 `stack_incomplete`，共同有效评分只有 3 题，远低于 20 题门槛，因此 `insufficient_common_coverage`，不能判断 DeepSeek 与 GPT 的能力胜负。能够确认的是官方 DeepSeek API 与 OpenCode/E2B 公共链兼容，且冻结前 7/7 任务可交付；不能把后续网络故障当作业务失败，也不能把该不完整 cohort 用于默认 solver、路线、训练、release 或 promotion 决策。

### R8.8 网络恢复后的全新同质 cohort

R8.8 不续跑 R8.7 的 14 个排除任务，也不混入其 7 份 solver 交付或 3 份评分。旧 scope、receipt、execution、audit 和 result 全部保持只读。在用户确认网络恢复后，使用已修复的 DNS/stream 分类器建立新 scope，从官方 provider 和公共 XLSX 探针开始，对 24 题全部重新执行。

恢复 campaign 仍固定 E2B 官方 `opencode` template、OpenCode 检测版本和官方 `deepseek-v4-pro`。matched 两题必须 2/2 后才扩展；每题一次项目级尝试，三次连续同类 infrastructure failure 立即冻结。执行后必须先生成新 execution audit，再对新 cohort 的有效交付从头进行一次性本地 GPT grader；旧 grader 记录不得复用。只有至少 20/24 有效且所有有效交付完成评分才可比较；否则停止，不自动开启第三轮。

R8.8 文档同步后本地 420/420 通过，但 14 MB 治理快照向 `serene-cloud` 的三次 `scp` 均在固定 120 秒内中断；每次半成品都已精确清理，远端未留下项目镜像。该上传上限现调整为 `scp -C` 与 600 秒，不改变快照内容、远端固定镜像、断网/只读/无凭据测试或清理边界。

传输修复后，定向测试 11/11、完整本地套件 421/421 与固定 Linux/amd64 parity 421/421 全部通过；parity source fingerprint=`4bb8c702...75e65`，report SHA=`8b1e9012...7f53b`，网络关闭、根目录只读、无 provider 凭据、cleanup 成功。新 scope SHA=`eb3c3960...50506` 被消费一次；R8.7 的 solver/grader 证据没有复用。

R8.8 的官方 provider、E2B 官方 `opencode` template、OpenCode 公开 XLSX 探针和 matched 2/2 门均通过。24 个全新独立 solver session 全部正常完成，24/24 在精确路径生成非空、可打开、有内容且不是输入副本的 XLSX；0 infrastructure failure、0 task delivery failure、0 post-freeze attempt。solver 总时长 3439.764 秒，中位单题 133.703 秒。execution SHA=`05dd9d2a...d452`；不可变审计 SHA=`ea7c3265...469c`，审计确认 24/24 counted、无冻结点、无 governance breach。

24 份有效交付随后各获得一次新的本地 `gpt-5.6-sol` 七维结构化评分，24/24 完成、没有低分重抽；grader manifest SHA=`630413f1...0bad`。DeepSeek 栈交付率为 24/24，但 professional plausibility 仅 14/24、major defect=6、均分 0.645833；冻结 GPT 基线为 24/24 plausibility、major defect=0、均分 0.803646。24 个共同任务中 GPT 22 胜、DeepSeek 1 胜、1 平，平均分差 0.157812；DeepSeek 总执行时间 3439.764 秒，快于 GPT 基线的 4556.608 秒，但质量差异达到预设实质门槛。两条生成路线在 DeepSeek 内部几乎持平：LLM-led 0.644792、skill-guided 0.646875，均为 7/12 plausibility 和 3 个 major defect。

最终 result SHA=`ea2a5163...ef8d`，decision=`official_deepseek_comparison_ready`，practical winner=`gpt_baseline`。这说明“官方 DeepSeek × OpenCode × E2B”执行链已经被证明可稳定完成任务，但在当前 24 题和固定 grader 下，GPT 基线的交付质量显著更高。该结论仅是 independent-model engineering proxy；professional validity 继续为 `provisional`，不自动切换默认 solver，不授权训练、release、registry 或 promotion。R8.8 至此关闭，不启动第三轮重复评测。

### R9 Huago-cone 内部生产发布与首批十题

R9 将主线从历史任务复评转向可重复生产：在 `huago-cone` 建立 SSH 驱动、可查询、可回滚的内部批处理服务，不增加 Web API。发布必须来自 `codex/r9-huago-cone-production` 的受控 Git commit；`.env`、provider key、Codex 认证、`artifacts/`、原始模型输出和 `Test/v2_outputs/` 永不进入 release context。

发布包含两个 Linux/amd64 镜像：factory 负责来源、技能、brief、proposal、物化与验证；eval 固定 Node 22、Codex CLI 0.146.0 和 OpenCode 1.17.13。容器只读根、单并发、上限 2 CPU/3 GiB。服务器只管理带 TaskGenerator label 的资源，保留 current/previous 与至少 100 GiB 空间。

首批 `ProductionTaskCohortV1` 从服务器重新抓取 PCAOB AS 1105/2201/2301 与 FAR Part 13/32.905/Part 46，使用 fresh-scratch registry，生成 5 道 audit/compliance 与 5 道 procurement/operations，统一 `llm_led_hybrid`，motif 为 4 cross-check、3 fan-in、3 policy-application。10/10 为 ready，8–9 可 partial 评测，少于 8 停止。人工专家与 Reality 暂不阻断，但证据固定为 `assumed_for_model_comparison`，不形成训练或公开发布权限。

随后比较 GPT-5.6/Codex、官方 DeepSeek/OpenCode 与 Gemini/Tuzi/OpenCode。每栈先过公开 XLSX probe；有效交付由 GPT 与 DeepSeek 两名匿名七维 judge 各评一次。R9 只输出模型差异和任务区分度，不自动切换 solver、训练或公开题库。

R9 发布与首轮生产现已执行。受控 release commit 为 `d70be7da0589caca4353f1a011e22642984f4d49`；完整本地回归 429/429、精确 secret scan 0 泄漏，最终 Linux/amd64 candidate 同样 429/429，并满足断网、只读根、无凭据和 cleanup。`huago-cone` 当前激活 `milestone-r9-huago-cone-094cbe481df3`，source fingerprint=`094cbe481df346a57387ff85c784d9ad62f1da78b2761e95a735fa081223ddf7`；factory/eval smoke 均通过，服务器剩余 137 GiB。由于这是首个 release，`previous` 暂与 `current` 指向同一版本，回滚机制可用但当前为安全 no-op。

首批 run `r9_huago_cone_first_production_10` 从服务器实时采集六个官方页面并保存独立 SHA/TLS 时间证据；fresh-scratch skill review 未修改 canonical registry。审计领域接受 3 个技能；采购领域既有 Tuzi extractor 出现一次非法 JSON、唯一格式补跑截断，官方 DeepSeek fallback 又返回空内容，因此改用服务器已验证的本地 Codex 对相同公开 FAR 文本按既有候选 schema 抽取，5/5 候选经确定性 review 接受。这个回退只替换公开 source-to-skill 提议者，没有修改任务设计模型或真实性门禁。

十个唯一 brief 已按 5+5 领域与 4/3/3 motif 正确编译。任务设计阶段严格执行 10 次 Tuzi `gpt-5.6-sol` 首调用、0 次无条件重抽；7 题 materialized，3 题在 provider 层以 `InternalServerError` 冻结，最终 `decision=production_insufficient`。generation result SHA=`f87b600c...bbb36`。7 个包全部通过 proposal、candidate/teacher isolation、deliverable contract、content、visual、rubric 和 rw-task export 门；31/31 候选 XLSX 可打开、非空，且与 export 副本逐字节一致，integrity report SHA=`c47fb50e...e9d10`。成功分布为 audit 3、procurement 4；cross-check 2、fan-in 2、policy 3。

由于 7/10 低于预先固定的 8 题评测门槛，本轮没有启动 GPT、DeepSeek 或 Gemini solver probe、业务 session、双评委评分或模型排名，也没有用历史任务补足。R9 当前准确状态是：**生产主机和发布链已完成，首批真实生产因 Tuzi task-design provider 的三次基础设施错误而以 `production_insufficient` 收口**。现阶段不应继续扩建沙盒/agent 适配层；下一生产批应先决定是否以同一冻结 brief 开设一次新的 provider campaign，或把已验证的直接 Codex task-design 路径纳入新的同质合同，随后仍需从零达到至少 8 题才可开始模型差异评测。

随后按上述第一种最小恢复方案建立独立 campaign `r9_huago_cone_recovery_production_10`：只复用冻结的六个公开来源、scratch-reviewed skills 和十个 briefs，不复用首轮 proposal、package 或成功结果；新 campaign ID 产生十个新 blind task IDs。10 次 Tuzi `gpt-5.6-sol` 正常首调用全部通过，0 repair、0 provider failure，最终 `production_ready`。generation result SHA=`0d519c5d...91812`，cohort SHA=`77e7835f...15b6bd`。10/10 包通过全部确定性门，39/39 候选 XLSX 可打开、非空并与 rw-task export 副本逐字节一致；integrity report SHA=`37c31a1b...1ba16`。首轮 7/10 保留为历史 provider 诊断，但不进入评测 cohort。

R9 服务器 eval 路径现补充统一薄 runner：GPT 使用本地 Codex/ChatGPT 登录，DeepSeek 与 Gemini 使用 OpenCode 原生 provider；每个 stack 必须先过公开 XLSX probe，私有任务独立 workspace、单进程、30 分钟硬超时、零 orchestration retry，跨领域 canary 的基础设施失败会停止扩展。runner 在内存读取并正规化 CRLF secret，stdout/stderr 必须先按全部实际 secret 值脱敏再落盘。完整本地回归为 431/431；新的不可变 release/parity 尚待完成。

公开 probe 的当前诊断不会进入模型能力比较：GPT/Codex 已通过；官方 DeepSeek/OpenCode 在五分钟内无事件、无 stderr、无 XLSX而超时，且没有私有上传。Gemini 首次 shell 探针因 CRLF env 与 OpenCode 配置错误，在模型前失败；OpenCode 的错误输出曾展开 Tuzi credential，服务器该日志已立即清空，后续 Gemini 调用暂停，必须使用新 runner 的内存脱敏路径，且现有 Tuzi key 应轮换。不得把这两次基础设施诊断解释为任务或模型能力失败。

公共 staging 漏写 `TASK.md` 的问题随后被定位并修复：旧 DeepSeek 诊断实际是在搜索服务器上的历史公开探针，精确路径门禁正确拒绝了其交付。修复新增了 staging 回归，完整本地与最终 Linux/amd64 parity 均为 432/432。当前生产 release 已更新为 `milestone-r9-huago-cone-5d8f76146a38`，commit=`4e01a6679e2ef6d79e7ab4cdec08123d7d0306a8`，source fingerprint=`5d8f76146a38296e66754b9ff306492d9fb192f7e76a7c983c0ae7c5a093b35a`；factory/eval smoke 通过、previous 可回滚、服务器仍有 127 GiB。

在最终 release 和全新输出根上，三个真实公共 XLSX probe 已全部通过：GPT/Codex 24.096 秒、官方 DeepSeek/OpenCode 35.950 秒、Gemini/Tuzi/OpenCode 52.714 秒，三者均 returncode 0、stderr 为空、生成精确路径且可打开的非空 XLSX。尚无任何 recovery 私有包进入 solver：首次 GPT 私有执行请求被权限审查拒绝，要求用户明确点名授权把 `r9_huago_cone_recovery_production_10` 的十个 route-blind 包发送给三个外部栈。不得把公共 probe 通过改写为私有业务评测完成。

用户现已明确授权把该 recovery cohort 的十个私有 route-blind 包发送给 GPT-5.6/ChatGPT Codex、官方 DeepSeek V4 Pro/OpenCode 与 Gemini 3.1 Pro Preview/Tuzi OpenCode，并允许后续 GPT、DeepSeek 双评委评分。统一双评委 runner 与评测记录编译器已实现：只把候选要求、冻结 rubric、fact anchors 和有效交付 XLSX 放入匿名评分工作区；每位 judge 每份交付一次实质评分，仅 JSON/schema 类失败可格式补跑一次，程序复算七维加权分。Codex 临时认证链接在进程结束后强制删除，solver/grader manifest 均绑定 generation、stack、judge 与任务全集并对漂移 fail closed。完整本地回归为 435/435；尚需完成文档后 parity 和包含该评分器的新 release，之后直接执行私有 canary、全量 solver、双评委与聚合。

R9 的完整生产与比较现已收口。受控发布为 `milestone-r9-huago-cone-6c3ae7f21ba6`（commit `f5fa0da...`），本地和固定 Linux/amd64 无网络、只读根、无凭据 parity 均为 435/435；factory/eval smoke 通过，服务器 `current` 指向该 release、`previous` 保留 `5d8f...`、可用磁盘为 122 GiB。recovery cohort 的 10/10 新任务包和 39/39 candidate XLSX 保持原有完整性证据；三套 solver 均通过公开 probe、跨领域 canary 和全部十题，合计 30/30 精确、非空、可打开且非输入副本的 XLSX，零基础设施与业务交付失败。

两名 route-blind judge 对全部 30 份交付完成 60/60 次有效七维评分，哈希绑定的 evaluation record set SHA=`df0be942...de84`，确定性 comparison result SHA=`2a3b4f0c...b9a3`。三栈均达 10/10 双评分覆盖：GPT/Codex composite=`0.812500`、DeepSeek/OpenCode=`0.767500`、Gemini/Tuzi/OpenCode=`0.683125`；任一 judge major defect 分别为 0、0、2。GPT 与 DeepSeek 的均分差 `0.045` 小于预设 `0.05` 实质差异线，故为 `practical_tie`；Gemini 相对二者的差异满足预设差异门槛，但其 7/10 judge-disagreement 说明平均值不能掩盖评委分歧。十题中 9 题属 medium/high separation，结果为 `comparison_complete` 与 `usefully_discriminative`。这是一项“模型 × provider × agent stack”的工程比较，专业有效性仍为 `assumed_for_model_comparison`；不改变默认 solver、不导出训练数据、不启动 SFT/RL、不公开题库或 promotion。
