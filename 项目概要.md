# TaskGenerator 项目概要

> 状态：active / authoritative；更新：2026-09-07。
> 唯一宏观状态源；文档入口见 [docs/README.md](docs/README.md)。

## 项目目标与当前结论

自动制造真实、有自然复杂度、可解且可公平评分的职业任务，而不是建设独立模型排行榜。

当前已跑通“公开种子/规则 → 专业 Skill → 工作世界 → 任务挖掘 → 监督与 rubric → 静态/行为实验”的研究方法链。**能产出任务并让模型作答，但任务难度效果、rubric 公平性和稳定规模化生产仍未充分验证。** 当前研究对象是可移植、可复做、能持续产出高质量任务的方法。评分研究已阶段性收尾，暂停继续校准与扩展；全套评分验收不再阻塞另定范围的造题研究。process-first 是实施选择，不宣称其优于直接生成。不建设自动评分反馈优化器，现阶段不做 RL，后续外部实验仍须另定范围。

## 下一阶段：造题 harness（proposed）

2026-09-07 用户认可调研方向，并决定结束继续修复与验证旧批次的路线；旧结果与限制保留，不以旧批次全部通过作为推进前提。下一阶段拟复用自主工厂，将独立开发试做的成果与障碍接入监督/rubric 编译，并形成从候选材料可复算的数值依据。按任务、角色、版本隔离上下文；程序管理权限、依赖和预算，Agent 请求检查、咨询、返工与提交。作者考点/难点说明只是设计意图，试做者不读取；编译先独立重建依据，再参考本题开发试做，最终复核保持新会话。

**上述增量尚未实现或运行。** 已有末端试做、咨询、版本与会话管理不等于新的开发反馈链。研究重点是专业性、实质复杂度、公平 rubric、非重复产出、完整成本与人工介入；可复算不等于专业口径正确，多 Agent 不等于独立证据。调研事实、来源版本和局限见[调研记录](docs/research/agent_task_production_harness_20260907.md)，实现路线见[优化计划](docs/architecture/pipeline_reconstruction_optimization_plan.md)。本次仅整理文档和现有实现的阶段性提交，不授权新外部实验。

## 2026-09-07 阶段性整理检查

新增调研记录并同步活跃文档，区分已有实现、历史边界与 proposed 目标。本次未修改源码，纳入此前未提交的实现、测试和通用方法文件作为项目检查点。定向测试 **160 项通过**，正式完整回归 **468 项及 8 个 subtests 通过**；初次 pytest 默认临时目录发生 WinError 5，改用独立临时目录后通过，未改代码或忽略失败。原始运行结果不变；文档链接、范围与提交内容检查记录位于本机 ignored [整理检查包](artifacts/r10/r10_checkpoint_20260907/)。本次提交授权不包含推送、新实验或新编排实现，代码测试通过不提升职业质量结论。

## 最近已结束批次：Agent 自主造题闭环

**本批已结束，未达到四个验证位置全部通过的门槛：最终提交终审准入3/4，完整通过2/4。** 开发题完整通过，单列不计验证分母；采购价格验证保留uncertain，交付验收及审计一有保留通过。审计二完成初稿和一次咨询，但作者第3次启动的修订续轮在已进入语义执行后遭遇Codex账户使用额度耗尽，原生返回`turn.failed`，该题封存为incomplete，未进入挖掘、编译、终审或试做。不是专业终审fail，也不是本批时间/启动预算耗尽；不得重跑该终结会话或自动另开scope。

全批使用39/60次启动或续轮，正式批次时钟从2026-09-07 00:23:27至06:22:21（香港时间），约5小时59分；程序`completed`表示固定位置调度已结束，不表示五题质量全部合格。保留9次咨询、91条已完成意见处理、全部首次原稿和失败轨迹；最后一题咨询尚无完成的处理记录。任务手改0，但开发准备、监测及4次研究者只读收尾属于介入。终审通过的4个内部包（含开发）保留原rubric，价格验证包完整准入仍为uncertain；没有评分或排名。原生消耗见报告，完整后端请求数和实际账单未知。服务器只读核查未发现本批运行中的容器；无新模型调用，剩余预算不转作下一批。方法报告及最终失败证据见[本批方法报告](artifacts/r10/r10_agent_factory_batch_20260907/method_report.md)。所有专业结论仍为provisional / LLM-proxy，不能宣布小批次跨场景复验获得完整初步支持或稳定批量生产。

验证三（审计资料可靠性）完成9次正常启动、2次咨询和22条处理记录；终审pass、试做有效，研究者只读收尾给予有保留的provisional通过。作者补充时区和文件来源声明、修改组织政策窗口，编译修订5项计分边界；政策改短不解释为补得原缺失证据。候选完成19订单实质核对及具体条件意见，22公式副本重算无错误。终审反误报正确行号、监督措辞偏强、当地窗口末端及非必要背景引用仍保留为限制，未改题或评分。

验证二（采购交付验收）9次正常启动完成，2次咨询、27条处理记录；终审pass、试做有效，研究者只读收尾给予有保留的provisional通过。作者修正订单的未来授权引用，编译修订三项重复扣分边界；试做进一步发现监督未突出订单电气/滤材配置的证据缺口，并形成具体条件意见。题干悬空引用非必要公开背景、监督覆盖遗漏和终审可见性论证不足仍保留为限制，未改题或评分。

冻结验证一已完成9次启动、2次咨询和23条意见处理，终审合同接纳pass、试做交付有效，但研究者只读收尾为 **uncertain，不计完整通过**。主要分歧是候选题干允许假设性历史比较，而rubric把4.2%历史变动限定为额外敏感性分析；实际试做采用了该项作为主要归一化假设，暴露了候选可见义务与评分边界是否一致的问题。另有十一/十份材料的数量错误和试做来源独立性表述过强；不把回答缺点直接等同于任务缺陷。未修订或重跑验证一；详细证据见批次对应位置的只读收尾记录。

2026-09-07 另获授权的“完整闭环复验与冻结跨场景验证”先完成采购开发题的完整链条，再执行四个冻结验证位置。开发题使用9次启动、2次咨询、19条意见处理，保留9份材料及两次实际修订；形成6项任务要求、18项51分rubric，独立终审合同接纳pass，候选试做DOCX/XLSX交付有效，研究者只读收尾给予provisional准入。终审曾误把rubric当作候选可见自查材料，该理由未采纳；实际隔离试做及重算提供了独立可解性代理证据。原始表格无公式缓存，只读重算副本84个公式均有结果且无错误；原稿未改。终端stdin传参、跨轮diff和编译引用反馈仍有工具摩擦，不把完整开发题通过解释为批量方法获证。

固定验证顺序为采购价格、交付验收及两个审计资料可靠性世界；最多60次启动/续轮、总时钟15小时，每题12次/3小时，终审与试做各预留一次及30分钟。方法在开发题前冻结，验证不按开发结果调参或重抽。职业输入按种子来源筛选并明确原来源ID映射；11个公开来源条目只按已读取摘要及适用范围解释，旧DAU目录页的WSC031归因未重核，已在新输入中限制使用。公开资料不证明合成业务事实真实或专家有效性。定向53项、完整468项及8个subtests通过；五个位置的断网文件检查、原生工具交接和finish当轮纠错通过。批次证据位于 `artifacts/r10/r10_agent_factory_batch_20260907/`，旧scope的incomplete状态不变。

另行批准的单题复验已结束，状态为 **incomplete：自主生产和最终提交完成，终审输出合同未通过，未试做、未导出准入包**。Agent 将 11 份初稿材料修订为 13 份，独立挖掘 6 项要求，形成监督和 8 项、37 分 rubric；主动进行两次独立咨询，登记 17 条绑定快照的意见处理。材料和 rubric 均有实际修订，首次不合约的编译快照也保留，主对话没有代修业务文件。共用 8/12 次启动或续轮、正式总时钟 72 分 49 秒、技术恢复 0 次。

一次性终审正常返回原文 pass，但 31 条 finding 中 26 条 path 合写多个文件、使用缩写或目录，不符合单文件引用合同，控制器拒绝接纳；这不是专业 fail 或 Provider 故障。原稿、首次停止 receipt 和执行版保持不变，没有重跑终审或启动试做。上轮交接说明泄漏未在本次重现，角色与版本检查、当轮编译纠错及咨询处理得到真实运行证据，但完整验收仍未通过。详细失败、修订、消耗和限制见[单题复验报告](artifacts/r10/r10_agent_factory_revalidation_20260906/report.md)。

运行前定向 **35 项**、完整 `pytest tests` **450 项及 8 个 subtests**及断网无凭据工具检查通过。停止后补充只读角色的 `finish` 当轮检查、单文件引用查询及具体字段错误反馈，避免只在会话结束后拒绝结果；定向 **39 项**、最终完整回归 **454 项及 8 个 subtests**通过。补丁仅离线验证，停止后无新模型调用；下一轮仍须另定范围，不把局部闭环证据升级为稳定批量生产或职业质量获证。

上一轮 2026-09-06 的采购开发世界按隔离停止条件结束，状态为 **incomplete；自主咨询与局部修订已观察到，完整造题闭环未通过**。世界作者形成 7 份候选文件，主动进行定位读取、渲染/重算和结构检查，调用一次独立材料咨询，再恢复原会话修订估算表。咨询发现估算引用的价格来源与调查记录不符；作者将该项改为依据已有历史价格的计算，明确保留指数和调整假设的限制。初稿与修订稿均保存，没有由主控制器代修业务材料。

控制器随后把世界作者的交接说明传给独立挖掘者，其中包含生成意图和咨询修订历史。文件挂载隔离成立，但完整消息边界失效，因此主动停止第 4 次会话；这不是 Provider 失败或挖掘专业质量失败。共使用 4/12 次启动/续轮、正式执行含检查约 31 分钟，技术恢复 0 次；未进入编译、终审或试做，没有合格内部任务包。原始 scope/receipt、受影响输入及所有快照保留，未重跑或扩题。

交接边界当时完成离线修复：下游交接仅传目标角色，重新挖掘使用新会话；咨询反馈须绑定相同角色和当前快照，其他反馈在启动前拒绝。工具合同说明中的工作目录也已校正。该次定向测试 **22 项**、完整 `pytest tests` **437 项及 8 个 subtests**通过；在该次收尾时尚未经过新模型验证，后续另定单题复验及五位置批次见上文。该次容器已停，详细证据、消耗、限制及停止原因见 [自主闭环报告](artifacts/r10/r10_agent_factory_20260906/report.md)。历史后续复验建议不构成当前运行授权，局部修订不能证明方法优于旧流水线或已可批量生产。

## 前一批：方法开发与冻结复验

本轮已结束：一个新采购开发世界形成一次通用修订，冻结后复验采购价格、交付验收及两个独立审计资料可靠性世界。**四个冻结位置首次准入 2/4，未达到四题全部通过的预定门槛，不能报告跨场景复验获得完整初步支持。** 两道采购完成四阶段并获有保留的 provisional 准入；审计一 Miner 引用未来题干/合同，审计二 rubric 误用交付结构保留 ID，均被输出合同拒绝，后续依赖阶段跳过。审计二还存在系统快照含未来实际事件、明示筛选表达式与导出金额不一致的重大内容问题；两道审计未完成独立复核，不能记为 Judge 的专业 fail。

开发四个会话均返回，但开发题因编译与复核字段/引用问题未准入，不计入验证合格率。控制器误拒公开背景引用的问题保留首次失败并离线修复，未重跑挖掘。整个 scope 使用 18/24 个会话（开发 4、验证生产 13、采购试做 1），模型技术恢复 0 次，任务级原稿修改 0 次；三个验证依赖阶段及审计试做跳过，不补位。唯一采购试做交付 DOCX/XLSX 有效，完成关键计算和具体条件意见，提供有限可解性行为证据，不评分、不排名。

正式执行含传输与检查约 3 小时 29 分，原始响应及可得 token/费用分路径记录；完整请求数和实际整批账单未知。开发修复、一次方法修订和研究者只读准入均属于运行介入，不能用零任务手改宣称无人化。最近实现测试为定向 23 项、完整 `pytest tests` 415 项及 8 个 subtests 通过；2026-09-06 恢复仅完成离线检查与文档收尾，未重新调用模型或宣称新代码回归。原始/派生/内部包哈希与阶段隔离核查通过，服务器本轮容器已停，current 未变。报告、逐题证据及消耗见 [方法研究检查包](artifacts/r10/r10_task_method_20260905/method_report.md)。

研究同时关注任务的职业质量和固定方法的首次合格率、失败、成本、人工介入与跨场景复用。不能用修好的样例替代批量方法证据，也不要求过程化策略必须胜过另一策略。方法与运行细则分别见活跃优化计划和 Runbook；旧实验与评分暂停状态保持不变。

## 关键证据

- **R9/R7（历史）**：完成过十题生产及三模型评测；完全新生产复验因 provider 不稳定冻结在 5/10。保留证据，不恢复或拼接。
- **R10.8–R10.9（冻结）**：形成职业任务及模型交付；R10.9 四道普通/强化任务完成静态检查、三档校准和 8/8 Solver 交付，但正式双评委均有歧义，难度改良效果尚不可裁决。
- **R10.10 评分探索（已结束）**：三题独立评分完成 12/12。发现题干与 rubric 不一致、核验范围不足、合理表达边界不清。样本不足以证明模型排名或 evaluator 已验证；原 60 次队列不恢复。

## 历史结果：两题 Rubric V2

独立版本支持“一个专业判断 → 多个评分项”，每项包含要求依据、分值、得分边界、条件例外、等价表达和核验方式。

| 开发题 | 新 rubric | 独立审查 |
| --- | --- | --- |
| 审计：收入资料可靠性 | 9 项，30 分 | DeepSeek pass |
| 采购：价格合理性 | 8 项，25 分 | DeepSeek pass |

Terra medium 生成，官方 DeepSeek V4 Pro max 审查。四次正常语义任务完成，另一次冗余格式补跑已停止，总计五次尝试。程序结果为 `rubric_compilation_supported`，仅表示生成侧 LLM proxy 的初步支持。

**仍不建议未经澄清直接投入评分：**

- 审计把明确的“独立工作表”放宽为“等效分区”。
- 采购允许暂缓认定，但满分锚点偏向肯定结论；市场比较的部分分与满分表述也有张力。
- 部分条目仍捆绑多个成果，未清楚覆盖各种部分正确的组合。

模型审查结论与额外文字复核意见分别保留，不用 pass 掩盖问题。最新完整代码回归见本页收尾状态。

## R10.11 结果与停止点

```text
R10.11 稳定独立逐项评分
→ Rubric V2 编译可靠性
→ process-first World-First 受控 A/B
→ 单一业务因果难度干预与强弱 stack 区分验证
```

R10.11 已按停止条件结束，结论为 **incomplete，不通过评分基线**：

- 官方 DeepSeek V4 Pro 完成 17/17 主评分；全部身份/输入/rubric/交付哈希闭合，item 覆盖、整数分、证据路径和程序求和结构有效，无材料 incomplete。
- 主分严重饱和：17 份中 16 份为满分。GDPval 三 stack 均值分别为 Sol `1.000000`、DeepSeek Pro `0.938272`、Flash `1.000000`；R10.9 的 Sol 与 DeepSeek Pro 均为 `1.000000`。这些只是诊断，不能证明模型方向或任务质量；相反说明当前主 Judge/材料抽取协议几乎没有区分力。
- 第一个 Terra 哨兵在语义评分前因 Codex 严格 JSON Schema 拒绝而失败；允许的一次格式恢复错误地原样重发同一 schema，仍返回 400。0/4 哨兵产生有效复核，无法检验 0.10 分差与高权重冲突门槛。
- runner 已离线修正 strict schema 和恢复状态处理，但遵守调用上限，没有第三次调用、没有重跑主评分或扩大样本。第一次本地终结器错误、一次 provider 前 SSH 权限失败、两次 Terra schema 失败及两个 controller-only scope amendment 均保留。

该结果促成了 R10.12 的统一 Stirrup 校准，而不是恢复 R10.11 或直接进入生成实验。

## R10.12 结果与停止点

R10.12 已实现固定 Stirrup/E2B 解题、模型组预检、首次失败保留、进度证据、严格逐项评分及 shadow downward audit 的薄控制器。原 scope 的 GBK 失败及后续每个新 scope 都分开冻结，没有覆盖首次失败。诊断确认 Stirrup 曾将 provider 单次输出上限误当作 Agent 上下文窗口，导致频繁压缩和超时；拆分为 `8192` 输出上限与 `64000` 上下文窗口后，`gpt-5.4-mini` 诊断在约 7 分钟内完成 10 次模型请求、14 次工具调用并生成可打开工作簿，证明该次是持续工作并完成，而非空转。更早的 Sol 超时运行没有进度埋点，只能确认已进入语义阶段且未留下交付，不足以事后判定其闲置还是未完成。

按用户要求，最终校准未使用 Gemini，而是冻结 Tuzi 上的 `gpt-5.4-mini` / `gpt-5-mini` / `gpt-4o-mini` 统一 panel。3/3 预检通过，9/9 正式 Solver 均保留一次首次结果；强、中档 6 份交付有效，弱档 3 份虽调用 `finish`，但都是伪装成 `.xlsx`/`.docx` 的纯文本，因不可打开而按合同记为 delivery failure，不进入语义评分。官方 DeepSeek strict 主评分 6/6 结构有效：强档三题为 `0.884211 / 0.985507 / 0.777778`，中档为 `0.915789 / 0.985507 / 0.777778`，均值分别为 `0.882499` 和 `0.893025`。Terra 严格复核的 Tuzi 路由在正常请求及一次允许的恢复中均返回 401，0/3 哨兵有效。

因弱档无可评分交付、无法计算强弱语义分差，且跨 Judge 门槛未闭合，R10.12 最终冻结为 **incomplete / provisional LLM-proxy**，不支持“统一 harness + strict 评分已可作为造题测量工具”的结论。主分已不再全部饱和，但中档均值略高于强档，不能宣称模型排名。这是当时的阶段门槛；2026-09-05 调整为评分不确定限制行为结论，不阻止另定范围开展造题研究。

## R10.12-S1 补充模型校准结果

R10.12-S1 新建不可覆写 scope，以 `gpt-5.5`、`gpt-5.6-sol`、`glm-5.2`、`gpt-5.4-mini` 四个 Tuzi route 统一走 Chat Completions。四个 route 均在首次 Stirrup 预检中完成至少两次模型请求、E2B `code_exec`、工具结果回传、`finish` 和可读取文件；实际模型请求数依次为 `2 / 8 / 4 / 2`。这证明四个 route 与当前 Stirrup Chat 工具闭环兼容，不证明公开模型身份或能力排序。

正式 Solver 前的 Judge gate 未通过：服务器 `eval_tuzi.env` 与当前本地凭据哈希一致、权限为 600，当前 token 的模型目录也可见 Terra，但 `gpt-5.6-terra` strict 单项微型夹具在首次正常调用返回 HTTP 401。按预设停止条件，401 不属于纯格式恢复，campaign 立即冻结为 **incomplete**；12 次 GDPval Solver、主评分和哨兵评分均未启动。因此本轮既不能支持强锚点区分，也不能判定任务存在天花板。现有证据只把剩余故障收窄到 Terra Chat Judge 的认证/路由链路，不能据此断言具体根因。

## 产物与运行基础

本机 ignored 结果入口：[R10.12-S1B 正式补测](artifacts/r10/r10_12_model_supplement_b_20260904/)、[R10.12-D Terra 诊断](artifacts/r10/r10_12_terra_transport_diagnostic_20260904/)、[R10.12-S1 补充模型校准](artifacts/r10/r10_12_model_supplement_20260904/)、[R10.12 最终 no-Gemini panel](artifacts/r10/r10_12_stirrup_gptmini_panel_20260904/)、[`gpt-5.4-mini` 上下文拆分诊断](artifacts/r10/r10_12_gpt54mini_contextsplit_probe_20260904/)、[R10.12 原始 GBK 失败](artifacts/r10/r10_12_stirrup_calibration_20260904/)、[R10.11 评分收尾](artifacts/r10/r10_11_independent_grader_20260904/)、[两题检查包](artifacts/r10/r10_10_rubric_generation_20260903_closeout/)。各 scope、首次失败、进度、交付、评分、summary 与 receipt 分开保留；这些私有产物不进入共享 Git。

沿用 huago-cone；current 为 `milestone-r9-huago-cone-a2d8a5e35099`，image SHA 为 `3c6c76324780a911ff7b47bf52629280b4f2a49f26248540de049a61b9129363`，previous 为 `milestone-r9-huago-cone-746ff9b55f1b`。R10.11 开始前已只读核对 108 GiB 空间、Codex 0.149.1、OpenCode 1.17.13、LibreOffice 25.2.3.2，以及 DeepSeek、tu-zi、Codex current auth 的存在与 600 权限；未输出凭据，未重建或激活 release，也未重复昂贵公共探针。

历史任务、候选材料、Teacher Truth、旧 rubric、Solver 交付和评分均不变。GDPval 具体题目、rubric、答案与文件只作评测校准，绝不进入生成输入；Test/v2_outputs 保持 no-touch。

G2 核验范围合同修复时的正式代码回归为 **365 项通过，另有 8 个 subtests 通过**；后续评分收尾回归见下文。直接从仓库根执行无范围 `pytest` 会错误收集 ignored `artifacts/` 中的历史 release 快照；正式回归根为 `tests/`。

## R10.12-D / S1-B 结果与停止点

R10.12-D 使用同一抽象 strict 单项 packet 各调用一次：本地 Tuzi Terra 通过，服务器 Tuzi Terra 返回 401，服务器 `gpt-5.6-terra@chatgpt_codex` 通过。由于仅本地成功不足以支持服务器哨兵，S1-B 固定选择 Codex Terra；三条首次结果均保留，无重试或端点切换。

S1-B 四个 Tuzi Solver route 的轻量 Chat health 均一次通过，随后完成 12/12 首次 Stirrup session，未使用技术恢复。`gpt-5.5`、`gpt-5.6-sol`、`gpt-5.4-mini` 均为 3/3 有效交付；`glm-5.2` 为 0/3，分别因一次 completion length/context overflow 和两次语义阶段后的 400 invalid request 终止，不进入语义评分。各题请求数依次为：`gpt-5.5 50/8/5`、`gpt-5.6-sol 20/8/8`、`glm-5.2 3/1/2`、`gpt-5.4-mini 10/6/4`。

官方 DeepSeek 主评完成审计题三份有效交付，三者均为 `1.000000`。采购题首份主评连续三次都返回 51 个 item，但把同一个 rubric UUID 的一位字符写错，形成“一项缺失 + 一项多余”；正常调用及两次允许的纯格式恢复均未闭合。首次 controller 漏判、amendment、原输出和两次恢复均保留。格式恢复预算耗尽后，S1-B 冻结为 **incomplete / provisional LLM-proxy**；其余主评与全部哨兵未启动。

因此本轮不能分类强锚点区分或任务天花板。可确认的诊断是：三条 GPT route 能稳定产生可打开交付，GLM route 与当前 Chat/Stirrup 协议跨题不稳定；审计题在已完成的三份 strict 主评上仍完全饱和；长 UUID 的逐项输出本身成为评分合同的脆弱点。不得恢复该 scope 或用 UUID 模糊纠错改写冻结评分。

## R10.12-G1 / G2 评分器修复结果

G1 将 XLSX 从扁平 XML 改为结构化单元格、公式、LibreOffice 重算值、格式和公式依赖证据，已确定识别 mini 交付的 `Prepaid Summary!B9` 循环引用、明细范围自引用及其他候选可见缺陷。但三个不可覆写 scope 分别止于重算/输出截断和自由文本 locator 不合约，均没有有效新分数；旧 `1.000000` 未被覆盖。

G2 恢复受约束的 Agent-native 评分：Codex/OpenCode 自主读取匿名实际文件和同一份预生成 XLSX 证据，逐 rubric 返回结构化 evidence ref，由控制器生成 locator/citation、约束 satisfaction—score 并求和。三份冻结交付的证据构建 3/3 通过且原文件哈希闭合。第一份 Terra 正常 session 给出了完整逐项判断，但四个含 `each` 的满分项将核验范围声明为 `formula`，没有满足预设的 `all_rows` 门槛。该冲突属于评分语义/核验范围而非 JSON 格式，故未使用格式恢复，也未启动其余五次 Judge。

G2 按停止条件冻结为 **incomplete / provisional LLM-proxy**。当前可以确认 Agent 已能读实际工作簿，且结构化引用消除了自由文本 locator 失败；尚不能确认跨 Judge 一致性、已知缺陷能否稳定扣分或评分器可用于任务难度测量。

G2 后的离线合同修复已将单选 `verification_scope` 拆为正交的 `coverage` 与 `verification_methods`：完整覆盖可同时使用公式和重算方法，抽样公式仍不能获得范围性满分。冻结 G2 结果不变；新的 B scope 只完成无 provider 的 dry-run；其执行安排现已取消，不记为评分失败或完成。

## 2026-09-05：评分器阶段性收尾

评分扩展暂停，不执行 G2-B，不新建 G2-C，不调整 reference 门槛。G1/G2 原 incomplete 结果保持不变。G2 runner 已加入 CLI 和直接执行的暂停保护，只读展示既有 dry-run；收尾不调用模型。

保留 Agent 文件检查、XLSX 提取与重算、逐 rubric 给分和程序求和。coverage/methods 拆分仅通过离线测试；强制外部 reference 仍过于机械，合法引用与覆盖声明不能证明核验充分，跨 Judge 一致性和已知缺陷稳定识别仍未获证。

隔离保证需按实际实现解释：系统根只读，但 G2 workspace 为读写挂载。提示词要求只读不等于输入技术隔离；已检查的原文件哈希不构成全部运行时输入未变的证明。本次不补建隔离系统。

评分收尾当时仅登记下一方向，未执行：先解决两道自产开发题的要求追溯、条件满分路径和成果捆绑，再比较 World-First/process-first 的材料因果性与可解性，最后对通过质量检查的任务有限作答与评分。随后另获授权的离线检查见下一节。GDPval 内容不进入生成或策略选择；不足以支持的难度与区分结论仍保持不确定。

收尾验证：执行入口定向测试 6 项通过；正式完整回归 `pytest tests` 为 369 项通过、8 个 subtests 通过。引用的 14 个历史证据文件哈希未变。收尾清单与引用哈希见 [评分收尾记录](artifacts/r10/r10_12_grader_closeout_20260905/)。本次不提交或推送 Git；测试通过不等于评分器可靠性验收通过。

## 2026-09-05：两道自产题离线要求追溯

经用户另行确认，完成两张候选要求—材料—结论—评分项对照表、16个局部假想边界案例及独立修订建议。仅使用两道自产开发题，先读候选材料，再对照监督和 Rubric V2；未读取历史 Solver 作答、分数或 GDPval 内容，外部调用为0。结果是 **检查完成、原 rubric 仍需修订**，不是题目准入、评分可靠性或自动生成改良获证；当前 Codex 复核仍为 provisional / LLM-proxy，并非独立专家验收。

- 原有四表/分区、条件满分、成果捆绑问题得到具体处理建议；进一步定位审计4/5分重叠、0/1分冲突及跟进成果重复计分风险。采购暂缓路径需同时贯穿价格结论和授奖建议；两份报价对基准的显式比较义务也需完整承接。
- 候选事实复算支持原有17项已列、12项遗漏及采购IGCE金额。新增记录审计材料的时间/来源链未解关系；不能判定是业务异常还是生成瑕疵，也不能据此宣布整题不可解。采购IGCE复用历史PO，不能把两者视为完全独立价格证据；有依据的保留判断不应受隐藏肯定结论约束。
- 原题、候选文件、监督和旧rubric均未改；31个绑定输入及14个评分收尾引用哈希复核不变。仅完成本地事实、链接、状态、范围和diff等检查，没有新代码回归，最近完整回归仍为上述369项及8个subtests。

检查包：[离线要求追溯与修订建议](artifacts/r10/r10_requirements_traceability_20260905/revision_recommendations.md)，范围、事实复算及收尾receipt同目录。具体事实和逐项处理只保留在ignored产物中。

本轮到此结束，不继续追求rubric绝对完美。后续可将process-first作为World-First内部的材料生成策略，重点比较事件、记录时点和来源关系是否自洽及候选是否可解。该离线检查授权不包含外部campaign；随后另获授权的材料实验见下节，评分暂停边界不变。

## 2026-09-05：采购材料小型对照

用户明确批准一个公开采购种子、两组材料的独立 scope：直接生成与先确定过程再物化；Sol high 生成、官方 DeepSeek V4 Pro max 挖掘及独立判断重建，固定顺序、最多六个串行会话、执行总时长最多三小时。不运行 Solver、评分 Judge 或排名实验。

新薄控制器及离线边界检查已实现。指定文件库及传递依赖已按 Linux/Python 3.12 锁定版本和 wheel 哈希，独立挂载，未覆盖镜像包；无凭据断网容器的 DOCX/XLSX/PDF 创建、读取、渲染及输入只读检查通过。正式 scope 已按首次失败条件停止为 **incomplete**：A/B 分别生成 12/11 份可读候选文件，两组匿名挖掘均找到自然任务；A 组独立复核原稿返回后，控制器误将合法任务/合同/规则引用判为越界，B 组复核未启动。五个会话正常返回，总执行含传输检查约 38.3 分钟，无重试或补跑。此停止点属于本轮控制器引用范围设计缺陷，不是模型认证或材料不可读失败。

首次证据支持 B 先冻结过程再物化，但过程化没有自动消除来源时点和要求覆盖的不确定性。两组 Miner 的“确立价格合理依据”等措辞仍需明确条件性完成路径；A 的独立复核也将“区间内余量”过度解释为算术中点，不能把代理意见直接升为专业事实。首次停止时 B 缺少独立复核，当时不支持方法优劣、难度或模型区分结论。

首次停止前回归为 383 项、8 个 subtests。保存执行代码快照后，对新薄控制器做了最小离线引用范围修正；当时未重写原 scope/receipt 或补跑。该次修正后定向 21 项、完整 `pytest tests` 390 项、8 个 subtests 通过，当时尚无修正后外部执行证据。首次材料、问题定位、限制与失败见 [首次比较报告](artifacts/r10/r10_process_first_pilot_20260905/comparison_report.md)。原题、评分暂停状态、current release 和既有暂存修改保持不变；随后新获授权的续跑见下节。

## 2026-09-05：授权续跑与整合收尾

用户明确授权自主测试、排错、修复并继续既定计划。新 continuation scope 绑定原材料、任务、提示词、依赖与剩余预算，只新增尚未开始的 B 组官方 DeepSeek V4 Pro max 复核；A 原稿仅离线复验，未重跑，未重生成或重挖掘。**六个计划会话已全部返回，两组复核均通过修复后的结构及引用检查。** B 的首次控制器检查另遇直接引用任务/规则文件名的问题，已在白名单内作精确解析并拒绝同名歧义，离线复验通过，没有第七个会话。两次原始 incomplete receipt 保留，联合完成状态另记。

两组均有明确下游用途，独立复核认为可以形成带限制的分析意见；这不等于无条件价格认定、授奖建议或专家有效性。B 虽有可核对的过程顺序，仍有证书日期窗口、来源依据和条件性表达不足；两组 Miner/复核的“确立依据”“完整”“完全可解”等措辞仍须受证据约束。当前最值得改进的是任务挖掘的要求依据和条件性完成路径，不能据此宣称 process-first 普遍优于直接生成。

该次定向测试 23 项、完整 `pytest tests` **392 项、8 个 subtests**通过。详见 [续跑整合报告](artifacts/r10/r10_process_first_continue_20260905/comparison_report.md)；原始响应、故障与恢复证据分开保留，全部为 ignored 研究检查包。结论仍为 provisional / LLM-proxy，无 Solver、评分 Judge、rubric 新编译、训练或发布，该次既定计划到此完成。
