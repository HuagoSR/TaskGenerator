# TaskGenerator 项目概要

> 文档状态：`active / authoritative`
> 更新日期：2026-09-03
> 职责：唯一项目级状态源，记录目标、当前证据、活跃研究主线和停止点。

## 项目目标

TaskGenerator 的目标是构建一个可追溯、可验证的职业任务生产系统：从公开专业材料和真实工作原型出发，生成候选可执行的多文件任务包、teacher-only 真值、可审计 rubric，并用模型行为证据检验任务价值。

项目的研究重点不是简单生成 prompt 或 benchmark，而是回答：能否稳定地把专业知识编译成真实、可解、不可投机且能区分模型能力的职业工作任务。

GDPval 仅用于任务形态与评测校准；其任务、隐藏 rubric 和内容不得进入生成或训练材料。

## 当前状态

R9 已证明受治理的服务器生产与评测链可以工作：huago-cone 可构建、激活、回滚 Linux/amd64 release；历史 recovery cohort 曾完成 10/10 新任务包、三模型 30/30 有效交付及双评委比较。该证据说明发布、物化、交付合同和模型执行链具有工程可行性。

随后完全从零的 R7 复验 `r9_huago_cone_fresh_reproduction_10_20260829_r7` 重抓六份公开来源、重建 skills、briefs 与十个新身份，但在 Tuzi task-design provider 不稳定时冻结为 5/10：五题 materialized，四题耗尽唯一允许的传输/格式补跑，第十题在连续 provider-side 失败时安全中断。R7 不构成部分评测 cohort，任何任务包、solver、grader 或 provider 结果不得与历史批次拼接。

对这五道已物化任务的人工目测显示：文件可打开、来源边界清楚、跨表关系可追溯，但任务通常只有少量记录，异常常由显式状态字段揭示，业务时间线和专业决策真值偏薄。当前瓶颈因此不再是“能否生成或运行任务包”，而是“如何让自动生成的任务真正来自可信的职业情景”。

本轮只读确认 huago-cone 为 `current=milestone-r9-huago-cone-a2d8a5e35099`、`previous=milestone-r9-huago-cone-746ff9b55f1b`，可用空间约 109 GiB；不修改 release。R9/R7 运行证据保持冻结。当前仅执行已授权的 R10.10 evaluator 校准，不恢复历史生产、训练、默认链切换、registry mutation 或公开题库发布。

## 当前与目标架构

当前实现仍由四层组成：

```text
Source / Skill / Resource Substrate
        ↓
Workflow / Motif / Task Planning
        ↓
Task Package Generation
        ↓
Validity / Evaluation / Feedback
```

R10 已将任务生成起点从 motif-first 改为 Scenario-First。R10.9 进一步将生成实验改为 World-First / Task-Mining：先生成并冻结工作世界，再由独立 Agent 从候选材料中发现自然任务。R10.10 现将活跃研究点移到 evaluator：在继续增加题量前，先验证评分协议能否稳定识别事实错误、职业灰区和重大专业错误。

```text
公开 Work Seed + Rules
→ Professional Skill 与自然难度辩论
→ 完整工作世界及自然业务记录
→ 独立 Task Mining
→ 独立 Teacher Truth 重建
→ Judge 校准
→ 多模型行为验收
```

专业 Skill 是可按需加载的知识包：它提供专业判断、工作步骤、常见严重错误及其来源，而不再决定题目模板、表格外形或业务故事。通用表格、文档和 PDF 操作由既有 Agent Skill 处理。Motif 仅保留为生成后的关系标签、分析维度和配额参考。

## 证据与边界

- 公开专业来源证明方法论边界，不自动证明虚构业务情景真实。
- candidate-visible 材料、teacher-only 监督和运行治理证据必须隔离。
- 静态结构、渲染和导出通过不等于职业真实性、专业有效性或训练价值。
- LLM 可以提出情景扩展、证据文件和表达；程序只保留必要的来源追溯、candidate/teacher 隔离、文件可打开性、答案泄漏、交付路径和状态变更检查。
- 任何外部模型调用、私有任务上传、训练、release 激活或 registry 变更仍需独立的受治理授权。

## R10 活跃研究主线

R10 是一个四题双领域 pilot：审计与采购各两题，所有真实工作种子仅来自公开、可追溯材料。

1. 固化 R9/R7 为回归样本和历史证据。
2. 建立 `WorkSeedV1`，记录角色、触发事件、业务目标、典型输入、自然问题、交付物和受众。
3. 建立 teacher-only `ScenarioBibleV1`，以完整业务世界作为唯一事实权威。
4. 在场景之后选择少量相关专业 Skill，由出题 Agent 派生独立证据包；异常必须由底层事实体现，不能以结论标签泄漏答案。
5. 编译自然任务、`TaskDecisionMatrixV1` 与 task-specific rubric。
6. 对四题同时进行一致性、可解性、泄漏和多模型区分度验收；全部通过后再决定是否扩展至十题。

R10.0 已完成离线合同地基。R10.1 现已完成公开 Work Seed 与专业规则准入：审计、采购各审查四个候选工作模式，确定性接纳各两个，所有来源均绑定官方 PCAOB 或 FAR 页面和具体段落引用。该轮只读取公开来源，不调用 provider、不生成 Scenario Bible 或候选任务。准入报告为 `pass`，接纳的四个种子是“公司自产信息可靠性”“控制缺陷组合评估”“简易采购价格合理性”和“商业交付验收处置”。

R10.1 的两个 FAR 页面经官方浏览器读取并记录为 `browser_verified`：直接程序化抓取受到 bot protection 阻断，未将其 403 响应冒充法规内容哈希；两个 PCAOB 页面则记录了实际内容 SHA。R10.2 现已完成：官方 DeepSeek 生成四份 teacher-only Scenario Bible，并通过事实唯一性、角色权限、规则绑定、时间顺序与背景/异常/未决问题配额的静态 admission。此前的 SDK/schema 与输出截断 run 均冻结为基础设施和编译合同诊断，不混入正式 Bible 证据。

R10.3 的投影就绪预检已经完成，并暴露了旧架构的局限：四个 Bible 是叙事型事实权威，不能被纯确定性投影器直接转为候选文件。该结论不要求建设复杂的 Bible V2，也不是单题质量失败。两个 factory-side 专业 Skill 已完成公开证据策展并升为 `curated`：各自具有规范、工作实践与失败模式来源，DeepSeek 仅在一次冻结公开来源批次中起草，所有非显然判断均保留来源绑定；旧 Registry 哈希保持不变。

R10.5 的服务器端运行环境已完成公开修复验证：原问题是受限 Docker 容器内的 Codex 又以 Bubblewrap 启动 Shell，嵌套 user namespace 被拒绝；现改为由只读根、无额外 capability、`no-new-privileges`、资源上限和单一临时工作区组成的外层 Docker 边界，Codex 在其内使用非嵌套 shell 模式。公开 Codex/Terra probe 已实际创建并独立验证 XLSX、DOCX、PDF；未启用特权容器、Stirrup 或另一套 agent。

R10.5 的早期两次私有 A/B cohort 因过窄的 extension 事实合同冻结为 `incomplete`，未产生 Skill 效果结论。修复后，extension registry 只要求安全、唯一的 ID 与 statement，并允许保留不参与闭合判断的 Bible-like 上下文；它不是 Bible V2。

自动 Skill Compiler 已完成并运行：GPT-5.6 Sol/Codex 基于公开来源、Seed、Rules 与历史反馈生成两个完整专业 Skill 包，官方 DeepSeek 分别通过其来源与内容审查。最新完整 cohort `r10_skill_review_ab_20260830_execute3` 的四个 Terra session 全部通过隔离、可打开性、extension/evidence-map 闭合和答案泄漏检查；两次 DeepSeek 条件盲审也完成。审计与采购的有 Skill bundle 都获得至少两项具体、文件级的职业真实性/证据自然度/判断深度改进，聚合为 `skill_effect_supported`。这只是独立 LLM proxy 证据，不是人工专家有效性。

R10.6 与 R10.7A 已共同形成四题 Pilot，并完成用户形态验收。四题分别覆盖公司自产信息可靠性、控制缺陷组合评估、简易采购价格合理性和商业交付验收处置；候选树、题干、交付合同、Teacher Truth、决策矩阵与 task-specific rubric 均冻结并通过静态 admission。R10.7A 的两个新增 Skill 已由自动 Skill Compiler 生成、经 DeepSeek 独立审查后使用；未人工补写专业结论。

R10.7B 已完成：GPT-5.6 Sol/Codex 与官方 DeepSeek V4 Pro/OpenCode 均通过公开 XLSX/DOCX probe，并各完成四题有效交付。16 次 route-blind LLM proxy 评分均完整，且仅发生一次 schema/传输型格式补跑。原始结果为 `behaviorally_admitted`：两条栈均为 4/4 有效交付、四题均有双评委评分、两题出现可解释模型差异；平均复合分为 GPT 0.94、DeepSeek 0.84。

R10.8A 已识别并修正一处 teacher-side 算术锚点：采购验收题将 3/40 误判为“在 5% 容差内”，实际为 7.5%。候选文件、题干和两份既有 Solver 交付未变，原 R10.7B 结果冻结为含 `teacher_anchor_error` 的历史证据。新建 judge-only receipt 后，Teacher Anchor Audit 确认更正关系为“超过 5%”。

huago 的 Codex candidate 在多文件公开 probe 中仍会话无进展，故未激活。R10.8A 改由本机 ChatGPT 登录的 Codex CLI 0.149.1 执行 GPT Judge；公开复杂度 probe 实际产生工具事件、合法 JSON 与 `turn.completed`。四项重评（两份冻结交付 × GPT/DeepSeek 两位 Judge）均完成：GPT Judge 仅走本地 Codex，DeepSeek Judge继续走官方 DeepSeek/OpenCode。新聚合为 `behaviorally_admitted`：两种 Solver 均 4/4 有效交付、四题均获得双评委评分、无未解决 teacher-anchor conflict 或重复证据缺口；但仅控制缺陷题保留可解释差异，当前为 `low_model_separation`。专业有效性继续为 `provisional / LLM-proxy`。

R10.8B-1 已对上述冻结 records、行为聚合和四个任务绑定完成只读区分度诊断：两道采购题为 `saturated`，收入证据题为 `near_tie`，控制缺陷题因 DeepSeek 交付的两位 Judge 对重大错误结论不一致而为 `judge_ambiguous`。没有任务达到 `cleanly_discriminative`，cohort 结论为 `compiler_revision_candidate`。

R10.8B-2 已完成 GDPval 本地镜像的只读形态对照和最小 compiler 改良：GDPval 内容不会进入生成输入；当前 R10 候选材料以少量紧凑 TXT/CSV 为主，缺少正常业务量、跨材料处理和细粒度评分边界。改良只要求 evidence author 把材料作为自然工作过程副产物生成，并要求 task compiler 面向业务成果写题、明确评分边界与具体严重错误。四个既有任务完全冻结。已从收入证据可靠性和价格合理性两个原 Bible 派生两道新题；两份 evidence bundle 和两份任务均通过静态 admission，且已获用户检查确认。

R10.8 随后迁移到 huago-cone 固定 release。候选镜像完成断网只读 parity，并固定 Codex 0.149.1、OpenCode 1.17.13、LibreOffice 25.2.3.2 与 openpyxl 3.1.5。Tuzi 私有 campaign 与旧服务器账号的公开失败均保留为隔离的历史基础设施证据，不进入最终比较。

服务器切换到新的独立 ChatGPT 认证目录后，精简公开门完整通过：最小固定 JSON 请求和与正式评分同结构的 GPT Judge 均产生 `turn.completed`；既有 DeepSeek 与文件工具公开证据按源码、镜像和输出树 SHA 复用。新的私有 scope 绑定两道冻结 revision 任务，两模型均形成有效交付，8 项双评委评分全部完成。关机前的控制器切换保留了已完成调用，其余评分由服务器单并发后台队列完成；交付 SHA、任务身份和完成事件均在聚合前重新校验。

当前执行点：收入证据题两模型复合分均为 1.0，仍为 `near_tie`；价格合理性题为 1.0 对 0.4375，但两位 Judge 对 DeepSeek 交付的具体判断和严重错误边界不一致，分类为 `judge_ambiguous`。R10.8B-2 总结论为 `evaluator_revision_required`，不是 compiler 改良成功。按预先冻结的判定规则，R10.8 在此结束，不生产六道新题、不扩展十题，也不修改 registry、激活 release、训练或 promotion。专业有效性保持 `provisional / LLM-proxy`。

R10.9 World-First / Task-Mining pilot 已完成。两个领域均从公开 Seed/Rules 独立生成基础与职业对抗强化工作世界；4/4 世界通过静态检查，Task Miner 均能从冻结候选材料中发现自然任务，Truth Reconstructor 与独立 ledger audit 均通过。四题的三档完整交付校准也全部通过，两种 Solver 随后形成 8/8 有效交付。

正式 paired blind review 暴露了更深的评价问题：四道任务全部为 `judge_ambiguous`。两位 Judge 在若干题上偏好方向相反，并反复对同一交付是否构成重大专业错误采用不同边界。由于校准样本能通过而真实交付仍发生系统性歧义，说明三档合成样本过于清晰，尚不能验证 Judge 对灰区职业判断的稳定性。R10.9 正确结论为 `evaluator_revision_required`，既不能证明对抗难度有效，也不能据此否定 World-First。当前停止在 evaluator 层：不扩展十题，不把候选难度指导写入正式 Skill catalog，也不重抽任务、答案或评分。专业有效性保持 `provisional / LLM-proxy`。

R10.10 是当前活跃阶段。它把问题拆为 rubric、Teacher Truth、Judge 协议和 Solver/Agent 环境四层：原始 GDPval 依靠职业专家编写的细粒度 rubric、人类参考交付物和专家盲评；GDPval-AA 使用匿名配对、Judge panel 与跨大量任务的统计聚合；旧 `rw-task` 则是逐交付绝对打分，能作为 baseline，但不是 GDPval-AA 的复现。R10 当前只有 3–5 个宽判断点，并把事实正确性、专业判断和重大错误混在一起，固定配对顺序和四题小样本又放大了模型间的评分风格差异。

R10.10 的 Evaluator V2.0 与换序 runner 已实现。两道开发题均产生稳定结果，因此冻结 V2.0，未迭代 V2.1/V2.2。一次性留出验证中，采购强化题稳定；审计强化题的 DeepSeek Judge 在换序后由平局变为偏好一方，故留出集未全通过。不得根据这一结果继续调参，也不能宣布 `evaluator_validated`。

公开 GDPval 12 题的三模型 Solver 已全部完成，各为 12/12 有效交付，共 36 份；未启用 Luna、Gemini 或 Tuzi。2026-09-03 用户要求降低评分成本后，停止原 60 次 judge 队列的新任务派发，仅保留已完成评分并收回当时已启动的第六次评分，不追加模型调用、不重跑 Solver。当前只形成小样本初步诊断，不宣称完成排名验证。已确认条件性 rubric 被误扣分，以及相同交付在不同配对中的逐项评分漂移；原始 rubric、答案和评分均不改写。

评分前的代码复核修正了两个协议缺陷：模型身份从 Agent 可见配对编号/schema 中移除，由控制器补入；六组 sentinel 分层覆盖三个职业与开发/留出两组，以同评委反向展示测位置效应、同展示顺序换评委测 Judge 一致性，两个指标不再混算。这是尚未开始的 GDPval 评分协议修复，不是依据 R10 留出结果修改评分标准。旧绝对评分 baseline 尚未完成，DeepSeek 动态别名与排行榜具体版本的对应关系仍需核实；在补齐前，方向性排名只能作为有限证据。

## 文档与操作入口

### 当前执行切片：单份 rubric 小样本评分

按最新批准计划，R10.10 新建独立的三题开发集评分 campaign，复用九份既有交付。官方 DeepSeek V4 Pro `max` 主评九次，Terra `medium` 固定抽查三次；每份交付独立按 human rubric 给整数分，程序求和，不再输出整体偏好票。正常评分十二次，全局最多两次传输/格式补跑；不重跑 Solver，不恢复旧六十次队列。三题均分仅作探索性比较，抽查分数不混入排名，不能宣布 evaluator 已验证。继承 rw-task 的逐项计分思路，但不继承高分后仅允许降分的二次审查。

精简入口已实现，执行前本地与断网只读 Linux parity 均为 285/285 通过。首项 DeepSeek 审计评分运行约九分钟后正常产生最终 JSON 与 `step_finish/stop`，但本地解析器将工具输出内合法 U+2028 当作 JSONL 分隔符，误判未完成。按 canary 停止规则，实际仅启动一次 Judge session、零补跑，后续十一项未启动；当前正式接纳为 0/12，状态 `preliminary_scoring_partial`，尚无模型排名。原始响应完整保留，不重抽。离线已修复按 LF 解析，并补充文件路径格式说明；首份响应还把绝对路径、单元格位置和临时渲染路径混入 evidence_paths，不能直接作为已通过文件引用检查的评分。当前停止在解析与输出格式修复后，未重启队列、未改写原 receipt 或失败状态。

用户已确认修复并续跑这批精简评分。恢复路径仅在新 scope 中导入已正常结束的首项：将明确对应输入文件的绝对路径及位置说明规范化，保留原始引用映射，不修改 awarded、rationale 或适用性。离线核验首项 56 条评分完整，原始与规范化得分均为 0.926316；原一次调用继续计入总上限。其余十一项仍按九份 DeepSeek 主评、三份 Terra 抽查的冻结配置执行，不重评首项、不恢复旧六十项队列。

1. [`docs/README.md`](docs/README.md)：文档生命周期与阅读顺序。
2. [`docs/architecture/system_architecture.md`](docs/architecture/system_architecture.md)：当前实现与 R10 目标架构。
3. [`docs/architecture/global_interface_contracts.md`](docs/architecture/global_interface_contracts.md)：稳定合同摘要与未实现 R10 草案。
4. [`docs/architecture/production_mvp_definition.md`](docs/architecture/production_mvp_definition.md)：现行生产状态和 R10 准入边界。
5. [`docs/architecture/pipeline_reconstruction_problem_statement.md`](docs/architecture/pipeline_reconstruction_problem_statement.md)：真实性问题定义。
6. [`docs/architecture/pipeline_reconstruction_optimization_plan.md`](docs/architecture/pipeline_reconstruction_optimization_plan.md)：R10 活跃计划。
7. [`docs/operations/pipeline_reconstruction_runbook.md`](docs/operations/pipeline_reconstruction_runbook.md)：当前停止点与未来执行前置条件。

`docs/archive/` 保存历史阶段材料；它们不得覆盖本文件的当前结论。
